# M2a 판독 파이프라인 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시트 61페이지를 LLM으로 판독해 readings·ambiguities를 DB에 쌓고, 적대적 검토로 다듬고, ambiguity마다 도면 크롭을 만든다. 합격은 SPEC_v2의 사용자 결정 4건(Q1~Q4)에 대응하는 ambiguity를 파이프라인이 스스로 검출하는 것.

**Architecture:** 3단 단발 구조화 호출 — 시트 판독(sonnet, 61회) → 계열 통합(sonnet, 6회) → 적대적 검토(fable, 6회). 모든 호출은 `messages.parse(output_format=PydanticModel)`로 스키마를 강제하고, `(입력 sha + 프롬프트 버전 + 모델)` 캐시로 재실행을 무과금화한다. 크롭은 LLM이 준 용지 mm bbox를 M1의 `SheetFrame`으로 px 변환하는 결정론 경로.

**Tech Stack:** Python 3.12 · anthropic 1.2.0 · pydantic 2.13 · PIL · psycopg (전부 설치·검증 완료). 신규 의존성 없음.

**정본:** [설계서](../specs/2026-08-30-m2a-reading-design.md). §참조는 설계서 절 번호.

## Global Constraints

- **venv Python**: `.\worker\.venv\Scripts\python.exe` (3.12.6). `PYTHONUTF8=1` 필수. 텍스트 I/O에 `encoding="utf-8"` 명시.
- **비밀키**: `ANTHROPIC_API_KEY`·`SUPABASE_DB_URL` 값을 코드·보고서·로그·커밋에 **절대 노출하지 않는다.** `load_config()` 경유만.
- **API 호출 규약(실측 확정 2026-08-30)**:
  - 클라이언트는 반드시 `Anthropic(default_headers={"anthropic-workspace-id": <ANTHROPIC_WORKSPACE_ID>})` — identity-linked 키라 헤더 없으면 400.
  - 스키마는 **`output_format=PydanticClass`** 로 전달한다. `output_config={"format": Class}` 는 `TypeError: ModelMetaclass is not JSON serializable` 로 죽는다(실측).
  - 결과는 `response.parsed_output` (pydantic 인스턴스).
- **과금 규율**: 테스트는 **실호출 금지**(녹화 픽스처 사용). 실호출은 지정된 Step 에서만. 모든 실호출은 `usage.jsonl` 에 적재.
- **참조 원본 읽기 전용**: `data/samples/`·`D:\Projects\Inspection\`. 산출물은 `data/derived/`(gitignore).
- **DB 쓰기 범위**: 0003 적용 + `readings`·`ambiguities` insert/update + `assets`(크롭) upsert. 기존 M0·M1 행 수정 금지.
- `cli.py` 기존 구조 보존(callback, samples/db 서브앱, seed, convert, catalog).
- **테스트 개수**: 시작 **144 passed**. 각 태스크 기대값은 작성 시점 계산 — 디스패치 시점 실측이 우선(개수를 맞추려 테스트를 지우지 않는다).
- 주석·CLI 출력 한국어. **커밋 메시지 말미에 두 줄을 그대로** 붙인다:

  ```
  Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1
  ```
- 실행 브랜치: `feat/m2a-reading` (main 에서 분기).

### 태스크 순서

| Task | 산출물 | LLM 실호출 |
|---|---|---|
| 1 | 0003 스키마 + Pydantic 행 모델 | — |
| 2 | LLM 클라이언트·캐시·usage 로깅 | 스모크 1회(<$0.01) |
| 3 | 판독 스키마(pydantic) + 입력 조립기 | — |
| 4 | 시트 판독 단계 + `m3d read --sheet` | 1시트(<$0.05) |
| 5 | 계열 통합 + 적대적 검토 + `m3d review` | B계열(<$0.5) |
| 6 | 크롭 생성 `m3d crops` (결정론) | — |
| 7 | **시범 B계열 완주 → 사용자 승인 게이트** | B 전체(<$1) |
| 8 | 전 계열 확산 + 합격 판정(§8) | 전체(~$8) |

---

## Task 1: 0003 스키마 + Pydantic 행 모델

**Files:**
- Create: `supabase/migrations/0003_readings.sql`
- Create: `worker/src/m3d/reading/__init__.py`
- Modify: `worker/src/m3d/models.py` (행 모델 4종 추가)
- Test: `worker/tests/test_migration_0003.py`
- Test: `worker/tests/test_reading_models.py`

**Interfaces:**
- Consumes: `m3d.models`(M0 패턴), `m3d.db.discover_migrations`
- Produces:
  - migration `0003_readings` — `readings`·`ambiguities` 2테이블 + RLS
  - `m3d.models.READING_STATUSES: tuple` = `("확정","추정","검토지적")`
  - `m3d.models.AMBIGUITY_STATUSES: tuple` = `("대기","결정","잠정")`
  - `m3d.models.ReadingRow(BaseModel)` — `region: str`, `item: str`, `value_raw: str`, `unit: str | None`, `basis_ord: str`, `basis_page_no: int`, `basis_mm_bbox: list[float]`(len 4), `crosscheck: dict | None`, `status: Literal`, `round: int`
  - `m3d.models.AmbiguityOption(BaseModel)` — `label: str`, `basis: str`
  - `m3d.models.AmbiguityRow(BaseModel)` — `item: str`, `basis_ord: str`, `basis_page_no: int`, `mm_bbox: list[float]`, `options: list[AmbiguityOption]`(2~4), `model_impact: str`, `status: Literal = "대기"`

- [ ] **Step 1: `supabase/migrations/0003_readings.sql` 작성**

```sql
-- M2a: 판독 결과와 애매성 (설계서 §5).
-- readings 는 치수 SSOT 초안, ambiguities 는 질문 카드의 원천이다.

create table readings (
  id             uuid primary key default gen_random_uuid(),
  project_id     uuid not null references projects(id) on delete cascade,
  region         text not null,                     -- 계열 A~F
  item           text not null,
  value_raw      text not null,                     -- 원문 표기 그대로 (지식베이스 §2)
  unit           text,
  basis_sheet_id uuid not null references sheets(id) on delete cascade,
  basis_page_id  uuid references sheet_pages(id) on delete set null,
  basis_mm_bbox  jsonb not null,                    -- [x0,y0,x1,y1] 용지 mm
  crosscheck     jsonb,                             -- {expr, result, ok} 검산 (§2)
  status         text not null
                   check (status in ('확정','추정','검토지적')),
  round          int  not null default 1,           -- 1 시트판독 2 통합 3 검토
  created_at     timestamptz not null default now()
);

create table ambiguities (
  id             uuid primary key default gen_random_uuid(),
  project_id     uuid not null references projects(id) on delete cascade,
  item           text not null,
  basis_sheet_id uuid not null references sheets(id) on delete cascade,
  sheet_page_id  uuid references sheet_pages(id) on delete set null,
  mm_bbox        jsonb not null,                    -- 크롭 원천 (§4 원본 픽셀 좌표)
  options        jsonb not null,                    -- [{label, basis}] 2~4개
  model_impact   text not null,                     -- 선택에 따른 모델 차이
  crop_rel_path  text,                              -- crops 실행 후 채움
  status         text not null default '대기'
                   check (status in ('대기','결정','잠정')),
  created_at     timestamptz not null default now()
);

create index on readings(project_id, region);
create index on readings(project_id, status);
create index on ambiguities(project_id, status);

alter table readings    enable row level security;
alter table ambiguities enable row level security;

create policy "authenticated read" on readings
  for select to authenticated using (true);
create policy "authenticated read" on ambiguities
  for select to authenticated using (true);
```

- [ ] **Step 2: 실패하는 마이그레이션 테스트 작성**

`worker/tests/test_migration_0003.py`:

```python
"""0003_readings — 판독 스키마의 제약이 회귀로 사라지지 않게 고정한다."""

import re

import pytest

from m3d.config import REPO_ROOT
from m3d.db import discover_migrations

TABLES = ("readings", "ambiguities")


@pytest.fixture
def norm() -> str:
    path = REPO_ROOT / "supabase" / "migrations" / "0003_readings.sql"
    return re.sub(r"\s+", " ", path.read_text(encoding="utf-8"))


def test_discovered_in_order():
    versions = [m.version for m in discover_migrations(REPO_ROOT / "supabase" / "migrations")]
    assert versions == ["0001_init", "0002_convert", "0003_readings"]


def test_both_tables_created(norm):
    for t in TABLES:
        assert f"create table {t} (" in norm, t


def test_rls_enabled_and_select_only(norm):
    for t in TABLES:
        assert f"alter table {t} enable row level security" in norm, t
    assert norm.count("for select to authenticated using (true)") == len(TABLES)
    assert "to anon" not in norm


def test_status_domains_pinned(norm):
    assert "check (status in ('확정','추정','검토지적'))" in norm
    assert "check (status in ('대기','결정','잠정'))" in norm


def test_value_raw_and_bbox_not_null(norm):
    """§2 원문 보존 · §4 크롭 좌표 — 둘 다 필수다."""
    assert "value_raw text not null" in norm
    assert "basis_mm_bbox jsonb not null" in norm
    assert "mm_bbox jsonb not null" in norm


def test_options_and_impact_not_null(norm):
    """선택지·모델 영향 없는 ambiguity 는 질문 카드가 될 수 없다 (§4)."""
    assert "options jsonb not null" in norm
    assert "model_impact text not null" in norm


def test_ambiguity_defaults_to_waiting(norm):
    assert "status text not null default '대기'" in norm
```

- [ ] **Step 3: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_migration_0003.py -v
```

Expected: SQL을 Step 1에서 먼저 썼다면 전부 통과한다(0002 때와 같은 문서화된 순서). `test_discovered_in_order`만 실패하면 파일명을 확인한다.

- [ ] **Step 4: `worker/src/m3d/reading/__init__.py` 작성**

```python
"""[4] 판독 파이프라인 — LLM 호출·캐시·스키마 (설계서 §4)."""
```

- [ ] **Step 5: 실패하는 행 모델 테스트 작성**

`worker/tests/test_reading_models.py`:

```python
"""판독 행 모델 — DB 왕복 전에 스키마 위반을 잡는다."""

import pytest
from pydantic import ValidationError

from m3d.models import AmbiguityOption, AmbiguityRow, ReadingRow

BBOX = [10.0, 20.0, 30.0, 40.0]


def _reading(**kw):
    base = dict(region="B", item="슬래브 두께", value_raw="300", unit="mm",
                basis_ord="B01", basis_page_no=1, basis_mm_bbox=BBOX,
                crosscheck=None, status="확정", round=1)
    base.update(kw)
    return ReadingRow(**base)


def _ambiguity(**kw):
    base = dict(item="슬래브 기입두께 해석", basis_ord="B01", basis_page_no=1,
                mm_bbox=BBOX,
                options=[AmbiguityOption(label="콘크리트 순두께", basis="계획고 검산 일치"),
                         AmbiguityOption(label="포장 포함 총두께", basis="표기 위치")],
                model_impact="슬래브 상면 EL 이 48mm 달라진다")
    base.update(kw)
    return AmbiguityRow(**base)


def test_reading_accepts_valid():
    r = _reading()
    assert r.status == "확정" and r.round == 1


def test_reading_rejects_unknown_status():
    with pytest.raises(ValidationError):
        _reading(status="대충맞음")


def test_reading_requires_four_point_bbox():
    with pytest.raises(ValidationError):
        _reading(basis_mm_bbox=[1.0, 2.0])


def test_reading_value_raw_must_be_nonempty():
    """§2 — 근거 없는/빈 수치는 싣지 않는다."""
    with pytest.raises(ValidationError):
        _reading(value_raw="")


def test_ambiguity_accepts_two_options():
    a = _ambiguity()
    assert a.status == "대기" and len(a.options) == 2


def test_ambiguity_rejects_single_option():
    """선택지가 하나면 질문이 아니라 확정이다 (§4: 선택지 2~4)."""
    with pytest.raises(ValidationError):
        _ambiguity(options=[AmbiguityOption(label="하나", basis="근거")])


def test_ambiguity_rejects_five_options():
    with pytest.raises(ValidationError):
        _ambiguity(options=[AmbiguityOption(label=f"안{i}", basis="근거") for i in range(5)])


def test_ambiguity_option_requires_basis():
    """§4 — 각 선택지에 근거를 붙인다."""
    with pytest.raises(ValidationError):
        AmbiguityOption(label="라벨만", basis="")


def test_ambiguity_requires_model_impact():
    with pytest.raises(ValidationError):
        _ambiguity(model_impact="")
```

- [ ] **Step 6: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_reading_models.py -v
```

Expected: `ImportError: cannot import name 'ReadingRow' from 'm3d.models'`

- [ ] **Step 7: `models.py` 에 행 모델 추가**

기존 import 줄을 아래로 바꾼다:

```python
from typing import Annotated, Any, Literal

from pydantic import BaseModel, Field
```

파일 끝에 추가:

```python
READING_STATUSES = ("확정", "추정", "검토지적")
AMBIGUITY_STATUSES = ("대기", "결정", "잠정")

# 용지 mm 좌표 [x0, y0, x1, y1] — 크롭·근거 표시의 공통 형태 (설계서 §6)
MmBBox = Annotated[list[float], Field(min_length=4, max_length=4)]


class ReadingRow(BaseModel):
    """판독 결과 1건. 근거(시트·페이지·mm bbox) 없이는 만들 수 없다 (지식베이스 §8)."""

    region: str
    item: str
    value_raw: str = Field(min_length=1)      # 원문 표기 그대로 (§2)
    unit: str | None = None
    basis_ord: str                            # sheets.ord — id 는 삽입 시 해석
    basis_page_no: int = Field(gt=0)
    basis_mm_bbox: MmBBox
    crosscheck: dict[str, Any] | None = None  # {expr, result, ok}
    status: Literal["확정", "추정", "검토지적"]
    round: int = Field(default=1, ge=1, le=3)


class AmbiguityOption(BaseModel):
    """해석 선택지 — 라벨과 근거는 한 쌍이다 (§4)."""

    label: str = Field(min_length=1)
    basis: str = Field(min_length=1)


class AmbiguityRow(BaseModel):
    """애매성 1건 = 질문 카드 1장. 한 질문 = 한 요소 (§4)."""

    item: str = Field(min_length=1)
    basis_ord: str
    basis_page_no: int = Field(gt=0)
    mm_bbox: MmBBox
    options: list[AmbiguityOption] = Field(min_length=2, max_length=4)
    model_impact: str = Field(min_length=1)
    status: Literal["대기", "결정", "잠정"] = "대기"
```

- [ ] **Step 8: 통과 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests -q
```

Expected: **160 passed** (144 + migration_0003 6 + reading_models 9 + 기존 1 조정 없음).

- [ ] **Step 9: 실DB에 0003 적용**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe db apply
```

Expected: `적용 1건: 0003_readings`. 재실행 시 `적용할 마이그레이션이 없습니다`.

- [ ] **Step 10: 적용 확인 (읽기 전용)**

```powershell
$env:PYTHONUTF8='1'; & .\worker\.venv\Scripts\python.exe -c 'import psycopg
from m3d.config import load_config
cfg = load_config()
with psycopg.connect(cfg.require_db_url()) as conn, conn.cursor() as cur:
    cur.execute("select relname, relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname=''public'' and relname = any(%s)", (["readings","ambiguities"],))
    print("RLS:", cur.fetchall())
    cur.execute("select count(*) from readings"); print("readings:", cur.fetchone()[0])
    cur.execute("select count(*) from ambiguities"); print("ambiguities:", cur.fetchone()[0])'
```

Expected: `RLS: [('readings', True), ('ambiguities', True)]`, 두 테이블 모두 0행.

- [ ] **Step 11: 커밋**

```bash
git add supabase/migrations/0003_readings.sql worker/src/m3d/reading worker/src/m3d/models.py worker/tests/test_migration_0003.py worker/tests/test_reading_models.py
git commit -m "feat(db): 0003_readings — readings·ambiguities + 행 모델

- 지식베이스를 제약으로: value_raw NOT NULL(§2 원문 보존), mm_bbox NOT NULL(§4 크롭),
  options 2~4 각 근거 필수·model_impact 필수(§4 질문 카드 요건)
- RLS select-only, M0 패턴 계승. 실DB 적용·멱등 확인

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## Task 2: LLM 클라이언트·캐시·usage 로깅

**Files:**
- Create: `worker/src/m3d/reading/client.py`
- Create: `worker/src/m3d/reading/cache.py`
- Test: `worker/tests/test_reading_cache.py`
- Test: `worker/tests/test_reading_client.py`

**Interfaces:**
- Consumes: `m3d.config.Config`, `anthropic`, `m3d.samples.manifest.sha256_file`
- Produces:
  - `m3d.reading.client.PROMPT_VERSION: str` (= `"v1"`) — 캐시 키 구성요소
  - `m3d.reading.client.MODEL_READ: str` (= `"claude-sonnet-5"`), `MODEL_REVIEW: str` (= `"claude-fable-5"`)
  - `m3d.reading.client.PRICING: dict[str, tuple[float, float]]` — 모델 → ($/1M in, $/1M out)
  - `m3d.reading.client.build_client(cfg) -> anthropic.Anthropic` — workspace 헤더 포함
  - `m3d.reading.client.estimate_cost(model, in_tok, out_tok) -> float`
  - `m3d.reading.client.log_usage(cfg, dataset, record: dict) -> None` — `usage.jsonl` 적재
  - `m3d.reading.cache.cache_key(payload: dict) -> str` — sha256(정규화 JSON)
  - `m3d.reading.cache.cache_path(cfg, dataset, kind, key) -> Path`
  - `m3d.reading.cache.load_cached(path) -> dict | None`, `save_cached(path, data: dict) -> None`

- [ ] **Step 1: 실패하는 캐시 테스트 작성**

`worker/tests/test_reading_cache.py`:

```python
"""판독 캐시 — 재실행을 무과금으로 만드는 장치. 키가 헐거우면 낡은 결과를 쓴다."""

import json

from m3d.reading.cache import cache_key, load_cached, save_cached


def test_key_is_stable_across_dict_order():
    a = cache_key({"model": "s", "prompt": "v1", "sheet": "abc"})
    b = cache_key({"sheet": "abc", "prompt": "v1", "model": "s"})
    assert a == b


def test_key_changes_with_any_field():
    base = {"model": "s", "prompt": "v1", "sheet": "abc"}
    k0 = cache_key(base)
    assert cache_key({**base, "model": "f"}) != k0      # 모델 변경
    assert cache_key({**base, "prompt": "v2"}) != k0    # 프롬프트 버전 변경
    assert cache_key({**base, "sheet": "xyz"}) != k0    # 입력 변경


def test_key_is_hex_sha256():
    k = cache_key({"a": 1})
    assert len(k) == 64 and all(c in "0123456789abcdef" for c in k)


def test_save_load_roundtrip_utf8(tmp_path):
    p = tmp_path / "x.json"
    data = {"readings": [{"item": "슬래브 두께", "value_raw": "300"}]}
    save_cached(p, data)
    assert load_cached(p) == data
    assert "슬래브" in p.read_text(encoding="utf-8")   # ensure_ascii=False


def test_load_missing_returns_none(tmp_path):
    assert load_cached(tmp_path / "none.json") is None


def test_load_corrupt_returns_none(tmp_path):
    """손상 캐시는 무시하고 재호출한다 — 죽지 않는다."""
    p = tmp_path / "bad.json"
    p.write_text("{ not json", encoding="utf-8")
    assert load_cached(p) is None
```

- [ ] **Step 2: 실패하는 클라이언트 테스트 작성**

`worker/tests/test_reading_client.py`:

```python
"""LLM 클라이언트 — 워크스페이스 헤더와 비용 계산. 실호출은 하지 않는다."""

import json

import pytest

from m3d.config import load_config
from m3d.reading.client import (
    MODEL_READ,
    MODEL_REVIEW,
    PRICING,
    build_client,
    estimate_cost,
    log_usage,
)


@pytest.fixture
def cfg(tmp_path, monkeypatch):
    monkeypatch.setenv("SAMPLE_SOURCE_DIR", str(tmp_path))
    monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-ant-test")
    monkeypatch.setenv("ANTHROPIC_WORKSPACE_ID", "wrkspc_test")
    return load_config(env_file=tmp_path / "absent.env")


def test_client_sends_workspace_header(cfg):
    """identity-linked 키는 이 헤더가 없으면 400 — 실측 확인된 요건."""
    client = build_client(cfg)
    assert client.default_headers["anthropic-workspace-id"] == "wrkspc_test"


def test_models_match_architecture_doc():
    assert MODEL_READ == "claude-sonnet-5"
    assert MODEL_REVIEW == "claude-fable-5"


def test_cost_uses_per_million_rates():
    """sonnet-5 = $2/$10 per 1M."""
    assert estimate_cost(MODEL_READ, 1_000_000, 0) == pytest.approx(2.0)
    assert estimate_cost(MODEL_READ, 0, 1_000_000) == pytest.approx(10.0)
    assert estimate_cost(MODEL_REVIEW, 1_000_000, 1_000_000) == pytest.approx(60.0)


def test_cost_zero_for_unknown_model():
    assert estimate_cost("mystery", 1000, 1000) == 0.0


def test_all_used_models_have_pricing():
    for m in (MODEL_READ, MODEL_REVIEW):
        assert m in PRICING


def test_log_usage_appends_jsonl(cfg, tmp_path, monkeypatch):
    monkeypatch.setattr(type(cfg), "derived_dir",
                        property(lambda self: tmp_path / "derived"))
    log_usage(cfg, "ds", {"stage": "read", "model": MODEL_READ,
                          "in": 100, "out": 50, "cached": False})
    log_usage(cfg, "ds", {"stage": "read", "model": MODEL_READ,
                          "in": 0, "out": 0, "cached": True})
    lines = (tmp_path / "derived" / "ds" / "usage.jsonl").read_text(
        encoding="utf-8").strip().splitlines()
    assert len(lines) == 2
    first = json.loads(lines[0])
    assert first["cost_usd"] == pytest.approx(estimate_cost(MODEL_READ, 100, 50))
    assert "ts" in first
```

- [ ] **Step 3: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_reading_cache.py worker\tests\test_reading_client.py -v
```

Expected: `ModuleNotFoundError: No module named 'm3d.reading.cache'`

- [ ] **Step 4: `worker/src/m3d/reading/cache.py` 구현**

```python
"""판독 캐시 (설계서 D5) — (입력 sha + 프롬프트 버전 + 모델) 이 같으면 무호출.

개발 중 수십 회 재실행이 전제다. 키가 헐거우면 낡은 결과를 쓰고,
너무 빡빡하면 캐시가 안 맞아 과금된다 — 키 구성요소는 client.py 가 정한다.
"""

from __future__ import annotations

import hashlib
import json
from pathlib import Path

from m3d.config import Config


def cache_key(payload: dict) -> str:
    """dict 순서와 무관한 안정적 키."""
    blob = json.dumps(payload, ensure_ascii=False, sort_keys=True, separators=(",", ":"))
    return hashlib.sha256(blob.encode("utf-8")).hexdigest()


def cache_path(cfg: Config, dataset: str, kind: str, key: str) -> Path:
    return cfg.derived_dir / dataset / "llm-cache" / kind / f"{key}.json"


def load_cached(path: Path) -> dict | None:
    if not path.is_file():
        return None
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return None  # 손상 캐시는 무시하고 재호출한다


def save_cached(path: Path, data: dict) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(
        json.dumps(data, ensure_ascii=False, indent=1) + "\n",
        encoding="utf-8", newline="\n",
    )
```

- [ ] **Step 5: `worker/src/m3d/reading/client.py` 구현**

```python
"""Anthropic 클라이언트·비용 계산·사용량 로깅 (설계서 §2-1).

실측 확정 사항 (2026-08-30):
  - identity-linked 키는 요청마다 anthropic-workspace-id 헤더가 필요하다.
    SDK 의 ANTHROPIC_WORKSPACE_ID env 자동 인식은 api_key 경로에서 동작하지 않는다.
  - 구조화 출력은 messages.parse(output_format=PydanticClass) 로 넘긴다.
    output_config={"format": Class} 는 TypeError 로 죽는다.
"""

from __future__ import annotations

import json
from datetime import datetime, timezone

import anthropic

from m3d.config import Config

PROMPT_VERSION = "v1"          # 프롬프트를 고치면 올린다 — 캐시 키 구성요소
MODEL_READ = "claude-sonnet-5"    # 판독·통합 (아키텍처 §2)
MODEL_REVIEW = "claude-fable-5"   # 적대적 검토

# 모델 → ($ per 1M input, $ per 1M output)
PRICING: dict[str, tuple[float, float]] = {
    "claude-sonnet-5": (2.0, 10.0),
    "claude-fable-5": (10.0, 50.0),
}


def build_client(cfg: Config) -> anthropic.Anthropic:
    ws = cfg.require_anthropic_workspace_id()
    return anthropic.Anthropic(
        api_key=cfg.require_anthropic_api_key(),
        default_headers={"anthropic-workspace-id": ws},
    )


def estimate_cost(model: str, in_tokens: int, out_tokens: int) -> float:
    rates = PRICING.get(model)
    if rates is None:
        return 0.0
    return in_tokens / 1_000_000 * rates[0] + out_tokens / 1_000_000 * rates[1]


def log_usage(cfg: Config, dataset: str, record: dict) -> None:
    """usage.jsonl 에 한 줄 적재 — 지출을 눈에 보이게 한다."""
    path = cfg.derived_dir / dataset / "usage.jsonl"
    path.parent.mkdir(parents=True, exist_ok=True)
    row = {
        "ts": datetime.now(timezone.utc).isoformat(timespec="seconds"),
        **record,
        "cost_usd": round(
            estimate_cost(record.get("model", ""), record.get("in", 0), record.get("out", 0)), 6
        ),
    }
    with path.open("a", encoding="utf-8", newline="\n") as fh:
        fh.write(json.dumps(row, ensure_ascii=False) + "\n")
```

- [ ] **Step 6: `config.py` 에 Anthropic 접근자 2개 추가**

`require_db_url` 아래에 추가:

```python
    def require_anthropic_api_key(self) -> str:
        if not self.anthropic_api_key:
            raise ConfigError("ANTHROPIC_API_KEY 미설정 — .env 를 확인하세요.")
        return self.anthropic_api_key

    def require_anthropic_workspace_id(self) -> str:
        if not self.anthropic_workspace_id:
            raise ConfigError(
                "ANTHROPIC_WORKSPACE_ID 미설정 — identity-linked 키는 워크스페이스 "
                "헤더가 필수입니다 (설계서 §2-1)."
            )
        return self.anthropic_workspace_id
```

`Config` 데이터클래스 필드에 2개 추가(`supabase_db_url` 아래):

```python
    anthropic_api_key: str | None
    anthropic_workspace_id: str | None
```

`load_config` 의 `Config(...)` 생성부에 2줄 추가:

```python
        anthropic_api_key=_env("ANTHROPIC_API_KEY"),
        anthropic_workspace_id=_env("ANTHROPIC_WORKSPACE_ID"),
```

- [ ] **Step 7: 통과 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests -q
```

Expected: **172 passed** (160 + cache 6 + client 6).

- [ ] **Step 8: 실호출 스모크 1회 (<$0.01) — 구조화 출력 경로 확인**

```powershell
$env:PYTHONUTF8='1'; & .\worker\.venv\Scripts\python.exe -c 'from pydantic import BaseModel, Field
from m3d.config import load_config
from m3d.reading.client import MODEL_READ, build_client, estimate_cost

class Out(BaseModel):
    answer: str = Field(description="한 단어")

cfg = load_config()
r = build_client(cfg).messages.parse(model=MODEL_READ, max_tokens=64,
    output_format=Out, messages=[{"role": "user", "content": "ping 이라고만 답해"}])
print("parsed:", r.parsed_output.answer, "| in", r.usage.input_tokens, "out", r.usage.output_tokens)
print("cost: $%.6f" % estimate_cost(MODEL_READ, r.usage.input_tokens, r.usage.output_tokens))'
```

Expected: `parsed: ping ...` + 비용 $0.01 미만. 400 이 나면 워크스페이스 헤더/키를 확인한다.

- [ ] **Step 9: 커밋**

```bash
git add worker/src/m3d/reading/client.py worker/src/m3d/reading/cache.py worker/src/m3d/config.py worker/tests/test_reading_cache.py worker/tests/test_reading_client.py
git commit -m "feat(reading): LLM 클라이언트·캐시·사용량 로깅

- 워크스페이스 헤더 필수(실측 확정), output_format=PydanticClass 규약 주석에 기록
- 캐시 키 = 입력 sha + 프롬프트 버전 + 모델 — 셋 중 하나만 바뀌어도 무효화 테스트
- usage.jsonl 에 토큰·비용·캐시 히트 적재 (지출 가시화)
- 손상 캐시는 무시하고 재호출 (죽지 않는다)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## Task 3: 판독 스키마(LLM 출력) + 입력 조립기

**Files:**
- Create: `worker/src/m3d/reading/schema.py`
- Create: `worker/src/m3d/reading/inputs.py`
- Test: `worker/tests/test_reading_schema.py`
- Test: `worker/tests/test_reading_inputs.py`

**Interfaces:**
- Consumes: `m3d.models.{ReadingRow, AmbiguityRow, AmbiguityOption}`, `m3d.convert.frames.SheetFrame`, PIL
- Produces:
  - `m3d.reading.schema.SheetReadOut(BaseModel)` — `readings: list[LlmReading]`, `ambiguities: list[LlmAmbiguity]`
  - `m3d.reading.schema.LlmReading(BaseModel)` — `item`, `value_raw`, `unit: str|None`, `page_no: int`, `mm_bbox: list[float]`, `crosscheck: str|None`, `status: Literal["확정","추정"]`
  - `m3d.reading.schema.LlmAmbiguity(BaseModel)` — `item`, `page_no`, `mm_bbox`, `options: list[AmbiguityOption]`(2~4), `model_impact`
  - `m3d.reading.schema.RegionMergeOut(BaseModel)` — `readings`, `ambiguities`, `notes: list[str]`
  - `m3d.reading.schema.ReviewOut(BaseModel)` — `findings: list[ReviewFinding]`
  - `m3d.reading.schema.ReviewFinding(BaseModel)` — `target_item`, `verdict: Literal["기각","상태변경","신규애매성"]`, `reason`, `new_status: Literal|None`, `new_ambiguity: LlmAmbiguity|None`
  - `m3d.reading.inputs.resize_for_vision(png: Path, out: Path, long_side: int = 1568) -> tuple[int,int]`
  - `m3d.reading.inputs.tile_titleblock(png: Path, out: Path) -> None` — 우하단 표제란 영역 크롭
  - `m3d.reading.inputs.image_block(png: Path) -> dict` — base64 image content block
  - `m3d.reading.inputs.sheet_text_excerpt(json_path: Path, max_rows: int = 1200) -> dict` — 큰 시트는 h_mm 큰 순으로 상위 절단 + 절단 사실 표기

- [ ] **Step 1: 실패하는 스키마 테스트 작성**

`worker/tests/test_reading_schema.py`:

```python
"""LLM 출력 스키마 — 근거 없는 수치를 API 응답 단계에서 반려한다 (지식베이스 §8)."""

import pytest
from pydantic import ValidationError

from m3d.models import AmbiguityOption
from m3d.reading.schema import (
    LlmAmbiguity,
    LlmReading,
    ReviewFinding,
    ReviewOut,
    SheetReadOut,
)

BBOX = [10.0, 20.0, 30.0, 40.0]


def _r(**kw):
    base = dict(item="슬래브 두께", value_raw="300", unit="mm", page_no=1,
                mm_bbox=BBOX, crosscheck=None, status="확정")
    base.update(kw)
    return LlmReading(**base)


def _a(**kw):
    base = dict(item="두께 해석", page_no=1, mm_bbox=BBOX,
                options=[AmbiguityOption(label="순두께", basis="검산 일치"),
                         AmbiguityOption(label="총두께", basis="표기 위치")],
                model_impact="상면 EL 48mm 차이")
    base.update(kw)
    return LlmAmbiguity(**base)


def test_reading_valid():
    assert _r().status == "확정"


def test_reading_rejects_review_status():
    """검토지적은 3단계가 붙이는 상태 — LLM 판독은 확정/추정만 낸다."""
    with pytest.raises(ValidationError):
        _r(status="검토지적")


def test_reading_rejects_missing_bbox():
    with pytest.raises(ValidationError):
        _r(mm_bbox=[1.0, 2.0, 3.0])


def test_reading_rejects_empty_value():
    with pytest.raises(ValidationError):
        _r(value_raw="")


def test_ambiguity_needs_two_options():
    with pytest.raises(ValidationError):
        _a(options=[AmbiguityOption(label="하나", basis="근거")])


def test_sheet_read_out_allows_empty_lists():
    """판독할 게 없는 시트도 정상 — 빈 결과를 에러로 만들지 않는다."""
    out = SheetReadOut(readings=[], ambiguities=[])
    assert out.readings == [] and out.ambiguities == []


def test_sheet_read_out_roundtrip():
    out = SheetReadOut(readings=[_r()], ambiguities=[_a()])
    again = SheetReadOut.model_validate(out.model_dump())
    assert again.readings[0].item == "슬래브 두께"
    assert again.ambiguities[0].options[1].label == "총두께"


def test_review_finding_status_change_requires_status():
    """상태변경 판정인데 새 상태가 없으면 반쪽 지적이다."""
    with pytest.raises(ValidationError):
        ReviewFinding(target_item="X", verdict="상태변경", reason="단일 소스", new_status=None)


def test_review_finding_new_ambiguity_requires_payload():
    with pytest.raises(ValidationError):
        ReviewFinding(target_item="X", verdict="신규애매성", reason="상충", new_ambiguity=None)


def test_review_finding_rejection_needs_no_payload():
    f = ReviewFinding(target_item="X", verdict="기각", reason="원문 재확인 결과 정합")
    assert f.new_status is None and f.new_ambiguity is None


def test_review_out_empty_is_valid():
    assert ReviewOut(findings=[]).findings == []
```

- [ ] **Step 2: 실패하는 입력 조립기 테스트 작성**

`worker/tests/test_reading_inputs.py`:

```python
"""LLM 입력 조립 — 8000px 원본을 비전 한도에 맞추고 sheet_text 를 안전하게 자른다."""

import base64
import json

from PIL import Image

from m3d.reading.inputs import (
    image_block,
    resize_for_vision,
    sheet_text_excerpt,
    tile_titleblock,
)


def _make_png(path, w=8000, h=5650):
    Image.new("RGB", (w, h), "white").save(path)
    return path


def test_resize_caps_long_side(tmp_path):
    src = _make_png(tmp_path / "s.png")
    out = tmp_path / "r.png"
    w, h = resize_for_vision(src, out)
    assert max(w, h) == 1568
    assert abs(w / h - 8000 / 5650) < 0.01     # 종횡비 보존
    with Image.open(out) as im:
        assert im.size == (w, h)


def test_resize_does_not_upscale_small(tmp_path):
    src = _make_png(tmp_path / "s.png", 800, 600)
    out = tmp_path / "r.png"
    w, h = resize_for_vision(src, out)
    assert (w, h) == (800, 600)                # 작은 이미지는 그대로


def test_titleblock_tile_is_bottom_right(tmp_path):
    """표제란은 도면 우하단 — 원본 대비 작고, 우하단 픽셀을 포함해야 한다."""
    src = tmp_path / "s.png"
    img = Image.new("RGB", (1000, 800), "white")
    img.putpixel((995, 795), (0, 0, 0))        # 우하단 표식
    img.save(src)
    out = tmp_path / "t.png"
    tile_titleblock(src, out)
    with Image.open(out) as tile:
        assert tile.width < 1000 and tile.height < 800
        assert tile.convert("L").getextrema()[0] == 0   # 검은 픽셀 포함


def test_image_block_shape(tmp_path):
    src = _make_png(tmp_path / "s.png", 100, 100)
    block = image_block(src)
    assert block["type"] == "image"
    assert block["source"]["type"] == "base64"
    assert block["source"]["media_type"] == "image/png"
    base64.b64decode(block["source"]["data"])   # 디코드 가능해야 한다


def _write_sheet_text(path, n, drawing_no="C1"):
    rows = [{"x_mm": float(i), "y_mm": float(i), "h_mm": 1.0 + (i % 10),
             "kind": "TEXT", "text": f"t{i}"} for i in range(n)]
    path.write_text(json.dumps(
        {"schema": 1, "drawing_no": drawing_no, "page_no": 1,
         "paper_mm": [1189.0, 841.0], "scale": 100.0, "rotation_deg": 0,
         "fallback": False, "texts": rows}, ensure_ascii=False), encoding="utf-8")
    return path


def test_excerpt_keeps_all_when_small(tmp_path):
    p = _write_sheet_text(tmp_path / "s.json", 50)
    out = sheet_text_excerpt(p)
    assert len(out["texts"]) == 50
    assert out["truncated"] is False


def test_excerpt_truncates_by_text_height(tmp_path):
    """자를 때는 작은 글자부터 버린다 — 제목·치수 같은 큰 글자가 살아남아야 한다."""
    p = _write_sheet_text(tmp_path / "s.json", 3000)
    out = sheet_text_excerpt(p, max_rows=100)
    assert len(out["texts"]) == 100
    assert out["truncated"] is True
    assert out["total_texts"] == 3000
    assert min(t["h_mm"] for t in out["texts"]) >= 9.0   # 큰 글자만 남음


def test_excerpt_preserves_header_fields(tmp_path):
    p = _write_sheet_text(tmp_path / "s.json", 10, drawing_no="C0050304-030")
    out = sheet_text_excerpt(p)
    assert out["drawing_no"] == "C0050304-030"
    assert out["paper_mm"] == [1189.0, 841.0]
    assert out["scale"] == 100.0
```

- [ ] **Step 3: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_reading_schema.py worker\tests\test_reading_inputs.py -v
```

Expected: `ModuleNotFoundError: No module named 'm3d.reading.schema'`

- [ ] **Step 4: `worker/src/m3d/reading/schema.py` 구현**

```python
"""LLM 출력 스키마 (설계서 §4).

messages.parse(output_format=...) 에 그대로 넘어간다. 여기서 반려되는 응답은
근거 없는 수치이며, 지식베이스 §8("근거 없는 수치는 반려")의 집행 지점이다.
"""

from __future__ import annotations

from typing import Annotated, Literal

from pydantic import BaseModel, Field, model_validator

from m3d.models import AmbiguityOption

MmBBox = Annotated[list[float], Field(min_length=4, max_length=4,
                                      description="[x0,y0,x1,y1] 용지 mm")]


class LlmReading(BaseModel):
    item: str = Field(min_length=1, description="항목명(한국어)")
    value_raw: str = Field(min_length=1, description="도면 원문 표기 그대로")
    unit: str | None = Field(default=None, description="mm, m, EL 등")
    page_no: int = Field(gt=0, description="근거 페이지 번호")
    mm_bbox: MmBBox
    crosscheck: str | None = Field(
        default=None, description="검산식과 결과. 예: '구간합 70,000 = 전장 70,000 ✓'")
    status: Literal["확정", "추정"] = Field(
        description="교차 확인된 값만 확정. 단일 소스·유추는 추정")


class LlmAmbiguity(BaseModel):
    item: str = Field(min_length=1, description="애매한 요소 하나")
    page_no: int = Field(gt=0)
    mm_bbox: MmBBox = Field(description="질문 카드 크롭이 될 영역")
    options: list[AmbiguityOption] = Field(
        min_length=2, max_length=4, description="권장안을 첫 번째로")
    model_impact: str = Field(min_length=1, description="선택에 따라 모델이 어떻게 달라지는가")


class SheetReadOut(BaseModel):
    readings: list[LlmReading]
    ambiguities: list[LlmAmbiguity]


class RegionMergeOut(BaseModel):
    readings: list[LlmReading]
    ambiguities: list[LlmAmbiguity]
    notes: list[str] = Field(default_factory=list, description="병합·교차확인 메모")


class ReviewFinding(BaseModel):
    target_item: str = Field(min_length=1)
    verdict: Literal["기각", "상태변경", "신규애매성"]
    reason: str = Field(min_length=1)
    new_status: Literal["확정", "추정", "검토지적"] | None = None
    new_ambiguity: LlmAmbiguity | None = None

    @model_validator(mode="after")
    def _payload_matches_verdict(self):
        if self.verdict == "상태변경" and self.new_status is None:
            raise ValueError("상태변경 판정에는 new_status 가 필요하다")
        if self.verdict == "신규애매성" and self.new_ambiguity is None:
            raise ValueError("신규애매성 판정에는 new_ambiguity 가 필요하다")
        return self


class ReviewOut(BaseModel):
    findings: list[ReviewFinding]
```

- [ ] **Step 5: `worker/src/m3d/reading/inputs.py` 구현**

```python
"""LLM 입력 조립 (설계서 §4-1·§10 리스크).

원 교훈: 판독의 기준 소스는 sheet_text(무손실 텍스트)이고, 이미지는 배치·형상
확인용이다. 8000px 원본은 비전 한도를 넘으므로 리사이즈하되, 표제란은 별도 타일로
원해상도 근처를 유지한다.
"""

from __future__ import annotations

import base64
import json
from pathlib import Path

from PIL import Image

Image.MAX_IMAGE_PIXELS = None

VISION_LONG_SIDE = 1568          # Anthropic 비전 권장 상한
TITLEBLOCK_FRAC = (0.62, 0.78)   # 우하단 표제란 대략 영역 (x, y 시작 비율)


def resize_for_vision(png: Path, out: Path, long_side: int = VISION_LONG_SIDE) -> tuple[int, int]:
    """긴 변을 long_side 로 축소. 이미 작으면 그대로 복사한다(업스케일 금지)."""
    with Image.open(png) as im:
        w, h = im.size
        if max(w, h) <= long_side:
            im.convert("RGB").save(out)
            return w, h
        scale = long_side / max(w, h)
        nw, nh = max(1, round(w * scale)), max(1, round(h * scale))
        out.parent.mkdir(parents=True, exist_ok=True)
        im.convert("RGB").resize((nw, nh), Image.LANCZOS).save(out)
        return nw, nh


def tile_titleblock(png: Path, out: Path) -> None:
    """우하단 표제란 타일 — 도면번호·제목·척도가 작게 적혀 있어 원해상도가 필요하다."""
    with Image.open(png) as im:
        w, h = im.size
        box = (int(w * TITLEBLOCK_FRAC[0]), int(h * TITLEBLOCK_FRAC[1]), w, h)
        out.parent.mkdir(parents=True, exist_ok=True)
        im.crop(box).convert("RGB").save(out)


def image_block(png: Path) -> dict:
    """Anthropic 비전 content block."""
    data = base64.b64encode(png.read_bytes()).decode("ascii")
    return {"type": "image",
            "source": {"type": "base64", "media_type": "image/png", "data": data}}


def sheet_text_excerpt(json_path: Path, max_rows: int = 1200) -> dict:
    """sheet_text 를 컨텍스트에 맞게 자른다.

    자를 때는 글자 높이 내림차순으로 남긴다 — 제목·치수 같은 큰 글자가 먼저 살고
    잔글씨가 먼저 버려진다. 자른 사실과 전체 개수를 함께 실어 LLM 이 알게 한다.
    """
    payload = json.loads(json_path.read_text(encoding="utf-8"))
    texts = payload.get("texts", [])
    total = len(texts)
    truncated = total > max_rows
    if truncated:
        texts = sorted(texts, key=lambda t: -t.get("h_mm", 0.0))[:max_rows]
        texts = sorted(texts, key=lambda t: (-t["y_mm"], t["x_mm"]))
    return {
        "drawing_no": payload.get("drawing_no"),
        "page_no": payload.get("page_no"),
        "paper_mm": payload.get("paper_mm"),
        "scale": payload.get("scale"),
        "fallback": payload.get("fallback", False),
        "total_texts": total,
        "truncated": truncated,
        "texts": texts,
    }
```

- [ ] **Step 6: 통과 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests -q
```

Expected: **196 passed** (172 + schema 11 + inputs 9 + 기존 유지). 실제 수는 실측 우선.

- [ ] **Step 7: 실 데이터 스모크 (LLM 호출 없음)**

```powershell
$env:PYTHONUTF8='1'; & .\worker\.venv\Scripts\python.exe -c 'from pathlib import Path
from m3d.reading.inputs import resize_for_vision, sheet_text_excerpt
src = Path("data/derived/ab1-p4p5/png/B01_C0050304-005_p1.png")
out = Path("D:/Codex/Temp/claude/D--Projects-model3d-studio/17239b6f-413a-4eb6-befe-e9f80ec596a2/scratchpad/vis.png")
print("resize:", resize_for_vision(src, out))
ex = sheet_text_excerpt(Path("data/derived/ab1-p4p5/text/B01_C0050304-005_p1.json"))
print("texts:", ex["total_texts"], "truncated:", ex["truncated"], "scale:", ex["scale"])
big = sheet_text_excerpt(Path("data/derived/ab1-p4p5/text/A03_C0050302-001_p1.json"))
print("A03 texts:", big["total_texts"], "truncated:", big["truncated"], "남은:", len(big["texts"]))'
```

Expected: resize `(1568, …)`, B01 텍스트 수 출력, A03(6,628행)은 `truncated: True` + 남은 1200.

- [ ] **Step 8: 커밋**

```bash
git add worker/src/m3d/reading/schema.py worker/src/m3d/reading/inputs.py worker/tests/test_reading_schema.py worker/tests/test_reading_inputs.py
git commit -m "feat(reading): LLM 출력 스키마 + 입력 조립기

- 스키마가 근거를 강제: mm_bbox 4점·value_raw 비어있지 않음·선택지 2~4 각 근거
- ReviewFinding 은 판정과 페이로드 정합을 model_validator 로 검사
- 입력: 8000px→1568px 리사이즈(업스케일 금지)·표제란 타일·sheet_text 절단은
  글자 높이 내림차순(잔글씨부터 버림) + 절단 사실을 LLM 에 고지

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## Task 4: 시트 판독 단계 + `m3d read --sheet`

**Files:**
- Create: `worker/src/m3d/reading/prompts.py`
- Create: `worker/src/m3d/reading/sheet.py`
- Modify: `worker/src/m3d/cli.py` (`read` 명령 추가)
- Test: `worker/tests/test_reading_sheet.py`

**Interfaces:**
- Consumes: Task 1~3 전부
- Produces:
  - `m3d.reading.prompts.KB_EXCERPT: str` — 지식베이스 §1~§4 발췌(프롬프트 상수)
  - `m3d.reading.prompts.REGION_HINTS: dict[str, str]` — 계열 A~F 판독 지침
  - `m3d.reading.prompts.sheet_system(region: str) -> str`
  - `m3d.reading.sheet.read_sheet(cfg, dataset, page, *, force=False, client=None) -> tuple[SheetReadOut, dict]` — (결과, usage 레코드). `client=None` 이면 build_client
  - `m3d.reading.sheet.PageRef` — frozen dataclass: `ord: str`, `drawing_no: str`, `page_no: int`, `region: str`, `png: Path`, `text: Path`
  - `m3d.reading.sheet.list_pages(cfg, dataset, region=None, ord_=None) -> list[PageRef]`

- [ ] **Step 1: 실패하는 테스트 작성 (실호출 없음 — 가짜 클라이언트)**

`worker/tests/test_reading_sheet.py`:

```python
"""시트 판독 — 캐시·반려 재시도·usage 기록. 실 API 는 호출하지 않는다."""

import json
from pathlib import Path

import pytest
from PIL import Image
from pydantic import ValidationError

from m3d.config import load_config
from m3d.reading.schema import SheetReadOut
from m3d.reading.sheet import PageRef, list_pages, read_sheet

BBOX = [10.0, 20.0, 30.0, 40.0]
GOOD = {
    "readings": [{"item": "슬래브 두께", "value_raw": "300", "unit": "mm",
                  "page_no": 1, "mm_bbox": BBOX, "crosscheck": None, "status": "확정"}],
    "ambiguities": [],
}


class FakeUsage:
    input_tokens = 1000
    output_tokens = 200


class FakeResponse:
    def __init__(self, parsed):
        self.parsed_output = parsed
        self.usage = FakeUsage()
        self.stop_reason = "end_turn"


class FakeMessages:
    """호출 횟수를 세고, 준비된 응답을 순서대로 돌려준다."""

    def __init__(self, outcomes):
        self.outcomes = list(outcomes)
        self.calls = 0

    def parse(self, **kwargs):
        self.calls += 1
        out = self.outcomes.pop(0)
        if isinstance(out, Exception):
            raise out
        return FakeResponse(SheetReadOut.model_validate(out))


class FakeClient:
    def __init__(self, outcomes):
        self.messages = FakeMessages(outcomes)


@pytest.fixture
def cfg(tmp_path, monkeypatch):
    monkeypatch.setenv("SAMPLE_SOURCE_DIR", str(tmp_path))
    monkeypatch.setenv("ANTHROPIC_API_KEY", "sk-ant-test")
    monkeypatch.setenv("ANTHROPIC_WORKSPACE_ID", "wrkspc_test")
    cfg = load_config(env_file=tmp_path / "absent.env")
    monkeypatch.setattr(type(cfg), "derived_dir",
                        property(lambda self: tmp_path / "derived"))
    return cfg


@pytest.fixture
def page(cfg):
    base = cfg.derived_dir / "ds"
    png = base / "png" / "B01_C1_p1.png"
    txt = base / "text" / "B01_C1_p1.json"
    png.parent.mkdir(parents=True, exist_ok=True)
    txt.parent.mkdir(parents=True, exist_ok=True)
    Image.new("RGB", (800, 600), "white").save(png)
    txt.write_text(json.dumps({"schema": 1, "drawing_no": "C1", "page_no": 1,
                               "paper_mm": [1189.0, 841.0], "scale": 100.0,
                               "rotation_deg": 0, "fallback": False,
                               "texts": [{"x_mm": 1.0, "y_mm": 2.0, "h_mm": 5.0,
                                          "kind": "TEXT", "text": "300"}]},
                              ensure_ascii=False), encoding="utf-8")
    return PageRef(ord="B01", drawing_no="C1", page_no=1, region="B", png=png, text=txt)


def test_reads_and_returns_parsed(cfg, page):
    client = FakeClient([GOOD])
    out, usage = read_sheet(cfg, "ds", page, client=client)
    assert out.readings[0].item == "슬래브 두께"
    assert usage["cached"] is False and usage["in"] == 1000
    assert client.messages.calls == 1


def test_second_call_hits_cache(cfg, page):
    client = FakeClient([GOOD])
    read_sheet(cfg, "ds", page, client=client)
    client2 = FakeClient([])          # 호출되면 IndexError 로 드러난다
    out, usage = read_sheet(cfg, "ds", page, client=client2)
    assert usage["cached"] is True and usage["in"] == 0
    assert client2.messages.calls == 0
    assert out.readings[0].value_raw == "300"


def test_force_bypasses_cache(cfg, page):
    read_sheet(cfg, "ds", page, client=FakeClient([GOOD]))
    client = FakeClient([GOOD])
    _out, usage = read_sheet(cfg, "ds", page, client=client, force=True)
    assert client.messages.calls == 1 and usage["cached"] is False


def test_schema_violation_retries_once(cfg, page):
    """근거 없는 수치는 반려하고 사유를 붙여 1회 재시도한다 (지식베이스 §8)."""
    bad = ValidationError.from_exception_data("SheetReadOut", [])
    client = FakeClient([bad, GOOD])
    out, usage = read_sheet(cfg, "ds", page, client=client)
    assert client.messages.calls == 2
    assert usage["retried"] is True
    assert out.readings[0].item == "슬래브 두께"


def test_schema_violation_twice_raises(cfg, page):
    bad = ValidationError.from_exception_data("SheetReadOut", [])
    client = FakeClient([bad, bad])
    with pytest.raises(ValidationError):
        read_sheet(cfg, "ds", page, client=client)


def test_usage_logged_to_jsonl(cfg, page):
    read_sheet(cfg, "ds", page, client=FakeClient([GOOD]))
    lines = (cfg.derived_dir / "ds" / "usage.jsonl").read_text(
        encoding="utf-8").strip().splitlines()
    rec = json.loads(lines[-1])
    assert rec["stage"] == "read" and rec["ord"] == "B01" and rec["cost_usd"] > 0


def test_list_pages_filters_by_region(cfg, page):
    pages = list_pages(cfg, "ds", region="B")
    assert [p.ord for p in pages] == ["B01"]
    assert list_pages(cfg, "ds", region="C") == []
```

- [ ] **Step 2: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_reading_sheet.py -v
```

Expected: `ModuleNotFoundError: No module named 'm3d.reading.sheet'`

- [ ] **Step 3: `worker/src/m3d/reading/prompts.py` 구현**

```python
"""판독 프롬프트 (설계서 §4-1).

프롬프트를 고치면 client.PROMPT_VERSION 을 올린다 — 캐시가 낡은 결과를 주지 않도록.
"""

from __future__ import annotations

KB_EXCERPT = """\
당신은 교량 실시설계 도면을 판독해 3D 모델링용 치수 정본을 만드는 전문가다.
아래 규칙은 실전에서 사고를 겪고 확립된 것이다. 반드시 따른다.

[좌표·단위]
- 도면 치수는 mm 정본이다. 원문 표기를 그대로 옮긴다(예: "2,800" 을 2800 으로 바꾸지 않는다).
- 근거 좌표는 주어진 sheet_text 의 용지 mm 좌표에서 유도한다.

[확정과 추정의 분리]
- 2개 이상의 뷰·시트에서 교차 확인된 값만 status="확정" 이다.
- 단일 소스이거나 유추가 섞이면 status="추정" 이다.
- 파생 계산(합계·환산)은 crosscheck 에 식과 결과를 적는다. 검산 없는 파생값은 내지 않는다.

[애매성은 추정으로 확정하지 않는다]
- 해석이 갈리는 값은 readings 에 넣지 말고 ambiguities 로 분리한다.
- 한 ambiguity = 한 요소. 선택지는 2~4개, 각각에 근거를 붙이고 권장안을 첫 번째에 둔다.
- model_impact 에 "이 선택에 따라 모델이 어떻게 달라지는지" 를 구체적 수치로 적는다.
- 서로 다른 뷰가 충돌하면 '서로 다른 구간(존)을 본 것' 일 가능성을 선택지에 포함한다.

[근거]
- 모든 readings·ambiguities 에 page_no 와 mm_bbox(용지 mm [x0,y0,x1,y1])를 단다.
- mm_bbox 는 그 값이 적힌 위치를 감싸는 영역이다. 질문 카드의 크롭이 되므로
  주변 맥락이 조금 포함되게 잡는다.
"""

REGION_HINTS: dict[str, str] = {
    "A": "일반도·종평면도·받침 배치. 전장·경간 분할·받침 형식과 EL, 좌표계 기준선에 집중한다. "
         "받침 형식은 도면 간 표기가 상충하는 사례가 있으니 교차 확인한다.",
    "B": "슬래브 일반도. 슬래브 두께 표기의 의미(콘크리트 순두께인지 포장 포함인지), "
         "캔틸레버 치수, 폭원 구성(차도·보도·방호벽)에 집중한다.",
    "C": "강상형 거더 일반도·상세도·단면도. 형고(내공인지 전강고인지), 판두께 전이, "
         "격벽 간격·타입, 종리브 열수(구간별로 다를 수 있다), 이음 위치에 집중한다.",
    "D": "외측가로보·외측빔. 부재 번호 체계와 위치 체인, 표준 타입 치수표에 집중한다.",
    "E": "부속 상세(출입구·맨홀·환기구 등). 위치와 개소수, 부착 상세에 집중한다.",
    "F": "하부공 교각 일반도. 받침 위치·솔플레이트·변화 치수표에 집중한다.",
}


def sheet_system(region: str) -> str:
    hint = REGION_HINTS.get(region, "")
    return f"{KB_EXCERPT}\n[이 계열의 판독 지침]\n{hint}\n"
```

- [ ] **Step 4: `worker/src/m3d/reading/sheet.py` 구현**

```python
"""시트 단위 판독 (설계서 §4-1) — 캐시·반려 재시도·usage 기록."""

from __future__ import annotations

import json
from dataclasses import dataclass
from pathlib import Path

from pydantic import ValidationError

from m3d.config import Config
from m3d.reading import prompts
from m3d.reading.cache import cache_key, cache_path, load_cached, save_cached
from m3d.reading.client import (
    MODEL_READ,
    PROMPT_VERSION,
    build_client,
    estimate_cost,
    log_usage,
)
from m3d.reading.inputs import (
    image_block,
    resize_for_vision,
    sheet_text_excerpt,
    tile_titleblock,
)
from m3d.reading.schema import SheetReadOut
from m3d.samples.manifest import sha256_file

MAX_TOKENS = 8000


@dataclass(frozen=True)
class PageRef:
    ord: str
    drawing_no: str
    page_no: int
    region: str
    png: Path
    text: Path


def list_pages(cfg: Config, dataset: str, region: str | None = None,
               ord_: str | None = None) -> list[PageRef]:
    """data/derived 의 페이지 산출물에서 판독 대상을 만든다. 계열은 ord 첫 글자."""
    base = cfg.derived_dir / dataset
    pages: list[PageRef] = []
    for png in sorted((base / "png").glob("*.png")):
        stem = png.stem                       # {ord}_{drawing_no}_p{n}
        ord_part, rest = stem.split("_", 1)
        drawing_no, page_part = rest.rsplit("_p", 1)
        if region and not ord_part.startswith(region):
            continue
        if ord_ and ord_part != ord_:
            continue
        text = base / "text" / f"{stem}.json"
        if not text.is_file():
            continue
        pages.append(PageRef(ord=ord_part, drawing_no=drawing_no,
                             page_no=int(page_part), region=ord_part[0],
                             png=png, text=text))
    return pages


def _build_messages(cfg: Config, dataset: str, page: PageRef, retry_reason: str | None):
    work = cfg.derived_dir / dataset / "llm-input"
    vis = work / f"{page.png.stem}.vis.png"
    tile = work / f"{page.png.stem}.title.png"
    resize_for_vision(page.png, vis)
    tile_titleblock(page.png, tile)
    excerpt = sheet_text_excerpt(page.text)

    parts = [
        {"type": "text", "text": f"도면 {page.drawing_no} {page.page_no}페이지 (계열 {page.region})."},
        image_block(vis),
        {"type": "text", "text": "표제란 확대:"},
        image_block(tile),
        {"type": "text", "text":
            "sheet_text (용지 mm 좌표, 이 값이 치수 판독의 1차 소스다):\n"
            + json.dumps(excerpt, ensure_ascii=False)},
        {"type": "text", "text":
            "이 시트에서 판독 가능한 치수를 readings 로, 해석이 갈리는 것을 "
            "ambiguities 로 내라."},
    ]
    if retry_reason:
        parts.append({"type": "text", "text":
            f"[재시도] 직전 응답이 스키마 검증에 실패했다: {retry_reason}\n"
            "모든 항목에 page_no 와 mm_bbox(4개 실수)를 넣고, 선택지는 2~4개로 하라."})
    return [{"role": "user", "content": parts}]


def read_sheet(cfg: Config, dataset: str, page: PageRef, *,
               force: bool = False, client=None) -> tuple[SheetReadOut, dict]:
    key = cache_key({
        "stage": "read",
        "png": sha256_file(page.png),
        "text": sha256_file(page.text),
        "prompt": PROMPT_VERSION,
        "model": MODEL_READ,
    })
    path = cache_path(cfg, dataset, "read", key)

    if not force:
        cached = load_cached(path)
        if cached is not None:
            usage = {"stage": "read", "ord": page.ord, "page_no": page.page_no,
                     "model": MODEL_READ, "in": 0, "out": 0,
                     "cached": True, "retried": False}
            log_usage(cfg, dataset, usage)
            return SheetReadOut.model_validate(cached), usage

    client = client or build_client(cfg)
    system = prompts.sheet_system(page.region)
    retried = False
    reason: str | None = None

    for attempt in (1, 2):
        try:
            resp = client.messages.parse(
                model=MODEL_READ, max_tokens=MAX_TOKENS,
                system=system, output_format=SheetReadOut,
                messages=_build_messages(cfg, dataset, page, reason),
            )
            break
        except ValidationError as exc:
            if attempt == 2:
                raise
            retried = True
            reason = str(exc)[:500]

    out: SheetReadOut = resp.parsed_output
    save_cached(path, out.model_dump())
    usage = {"stage": "read", "ord": page.ord, "page_no": page.page_no,
             "model": MODEL_READ, "in": resp.usage.input_tokens,
             "out": resp.usage.output_tokens, "cached": False, "retried": retried}
    log_usage(cfg, dataset, usage)
    return out, usage
```

- [ ] **Step 5: 통과 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests -q
```

Expected: **203 passed** (196 + sheet 7). 실측 우선.

- [ ] **Step 6: CLI 에 `read` 명령 추가 (DB 반영은 Task 5 에서 붙인다)**

`cli.py` import 에 추가:

```python
from m3d.reading import sheet as reading_sheet
```

`catalog` 아래에 추가:

```python
@app.command()
def read(
    dataset: str = typer.Argument(..., help="데이터셋 슬러그"),
    region: str = typer.Option(None, "--region", help="계열 필터 (A~F)"),
    sheet: str = typer.Option(None, "--sheet", help="시트 ord 필터 (예: B01)"),
    force: bool = typer.Option(False, "--force", help="캐시 무시하고 재호출"),
) -> None:
    """[4] 시트 판독 — readings·ambiguities 초안 (설계서 §4-1)."""
    cfg = load_config()
    pages = reading_sheet.list_pages(cfg, dataset, region=region, ord_=sheet)
    if not pages:
        typer.echo("대상 페이지가 없습니다 — convert 를 먼저 돌렸는지 확인하세요.")
        raise typer.Exit(code=1)

    typer.echo(f"판독 {len(pages)}페이지 (계열 {region or '전체'})")
    total_cost, failures = 0.0, []
    for i, page in enumerate(pages, start=1):
        try:
            out, usage = reading_sheet.read_sheet(cfg, dataset, page, force=force)
        except Exception as exc:
            failures.append((page.ord, f"{type(exc).__name__}: {exc}"))
            typer.echo(f"[{i}/{len(pages)}] {page.ord}p{page.page_no} 실패: {type(exc).__name__}")
            continue
        cost = 0.0 if usage["cached"] else __import__("m3d.reading.client", fromlist=["x"]).estimate_cost(
            usage["model"], usage["in"], usage["out"])
        total_cost += cost
        mark = "캐시" if usage["cached"] else f"${cost:.4f}"
        typer.echo(f"[{i}/{len(pages)}] {page.ord}p{page.page_no} "
                   f"readings {len(out.readings)} / ambiguities {len(out.ambiguities)} ({mark})")

    typer.echo(f"\n합계 비용 ${total_cost:.4f} / 실패 {len(failures)}건")
    for ord_, err in failures:
        typer.echo(f"  실패 {ord_}: {err}")
    raise typer.Exit(code=1 if failures else 0)
```

**주의**: 위 `__import__` 는 가독성이 나쁘다 — import 블록에 `from m3d.reading.client import estimate_cost` 를 추가하고 `estimate_cost(...)` 로 직접 부른다.

- [ ] **Step 7: 실호출 1시트 (<$0.05) — 판독 품질 첫 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe read ab1-p4p5 --sheet B01
```

Expected: 2페이지 처리, readings·ambiguities 개수와 비용 출력. 결과 JSON 을 눈으로 확인:

```powershell
$env:PYTHONUTF8='1'; & .\worker\.venv\Scripts\python.exe -c 'import json, glob, io
p = sorted(glob.glob("data/derived/ab1-p4p5/llm-cache/read/*.json"))[0]
d = json.load(io.open(p, encoding="utf-8"))
print("readings:", len(d["readings"]), "ambiguities:", len(d["ambiguities"]))
for r in d["readings"][:5]:
    print(f"  {r[\"item\"]} = {r[\"value_raw\"]} [{r[\"status\"]}] bbox={r[\"mm_bbox\"]}")
for a in d["ambiguities"][:3]:
    print(f"  ? {a[\"item\"]} — 선택지 {len(a[\"options\"])}, 영향: {a[\"model_impact\"][:40]}")'
```

- [ ] **Step 8: 커밋**

```bash
git add worker/src/m3d/reading/prompts.py worker/src/m3d/reading/sheet.py worker/src/m3d/cli.py worker/tests/test_reading_sheet.py
git commit -m "feat(reading): 시트 판독 + m3d read

- 프롬프트에 지식베이스 §1~§4 를 직접 실음(확정/추정 분리·애매성 미확정·근거 필수)
- 캐시 히트 시 무호출, --force 재호출, 스키마 반려 시 사유 붙여 1회 재시도
- 가짜 클라이언트로 캐시·재시도·usage 를 전부 무과금 검증

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## Task 5: 계열 통합 + 적대적 검토 + DB 반영 + `m3d review`

**Files:**
- Create: `worker/src/m3d/reading/region.py`
- Create: `worker/src/m3d/reading/store.py`
- Modify: `worker/src/m3d/reading/prompts.py` (통합·검토 프롬프트 추가)
- Modify: `worker/src/m3d/cli.py` (`read` 에 DB 반영 붙이고 `review` 추가)
- Test: `worker/tests/test_reading_region.py`
- Test: `worker/tests/test_reading_store.py`

**Interfaces:**
- Consumes: Task 1~4 전부
- Produces:
  - `m3d.reading.prompts.merge_system(region) -> str`, `review_system(region) -> str`
  - `m3d.reading.region.merge_region(cfg, dataset, region, sheet_outs, *, force=False, client=None) -> tuple[RegionMergeOut, dict]`
  - `m3d.reading.region.review_region(cfg, dataset, region, merged, *, force=False, client=None) -> tuple[ReviewOut, dict]`
  - `m3d.reading.region.apply_findings(merged: RegionMergeOut, review: ReviewOut) -> tuple[list[LlmReading], list[LlmAmbiguity], list[dict]]` — 순수: (최종 readings, 최종 ambiguities, 검토 기록)
  - `m3d.reading.store.replace_region(cfg, dataset, region, readings, ambiguities, round_no) -> dict[str,int]` — 계열 단위 delete+insert (멱등)
  - `m3d.reading.store.resolve_ids(conn, project_id) -> tuple[dict[str,str], dict[tuple[str,int],str]]` — ord→sheet_id, (ord,page)→page_id

- [ ] **Step 1: 실패하는 순수 로직 테스트 작성**

`worker/tests/test_reading_region.py`:

```python
"""적대적 검토 반영 — 지적을 어떻게 최종 결과에 녹이는가 (설계서 §4-3)."""

import pytest

from m3d.models import AmbiguityOption
from m3d.reading.region import apply_findings
from m3d.reading.schema import (
    LlmAmbiguity,
    LlmReading,
    RegionMergeOut,
    ReviewFinding,
    ReviewOut,
)

BBOX = [1.0, 2.0, 3.0, 4.0]


def _r(item, status="확정"):
    return LlmReading(item=item, value_raw="300", unit="mm", page_no=1,
                      mm_bbox=BBOX, crosscheck=None, status=status)


def _a(item):
    return LlmAmbiguity(item=item, page_no=1, mm_bbox=BBOX,
                        options=[AmbiguityOption(label="a", basis="b"),
                                 AmbiguityOption(label="c", basis="d")],
                        model_impact="영향")


def _merged(readings=None, ambiguities=None):
    return RegionMergeOut(readings=readings or [], ambiguities=ambiguities or [], notes=[])


def test_status_change_applied():
    merged = _merged([_r("두께")])
    review = ReviewOut(findings=[ReviewFinding(
        target_item="두께", verdict="상태변경", reason="단일 소스", new_status="추정")])
    readings, _amb, log = apply_findings(merged, review)
    assert readings[0].status == "추정"
    assert log[0]["applied"] is True


def test_new_ambiguity_appended():
    merged = _merged([_r("두께")], [_a("기존")])
    review = ReviewOut(findings=[ReviewFinding(
        target_item="두께", verdict="신규애매성", reason="상충",
        new_ambiguity=_a("신규"))])
    _readings, ambiguities, _log = apply_findings(merged, review)
    assert [a.item for a in ambiguities] == ["기존", "신규"]


def test_rejection_changes_nothing_but_is_logged():
    """기각도 기록한다 — 무엇을 왜 받아들이지 않았는지 남아야 한다 (§3)."""
    merged = _merged([_r("두께")])
    review = ReviewOut(findings=[ReviewFinding(
        target_item="두께", verdict="기각", reason="원문 재현 결과 정합")])
    readings, ambiguities, log = apply_findings(merged, review)
    assert readings[0].status == "확정" and ambiguities == []
    assert log[0]["verdict"] == "기각" and log[0]["applied"] is False


def test_unknown_target_is_logged_not_crashed():
    """존재하지 않는 항목을 지적해도 죽지 않고 미적용으로 기록한다."""
    merged = _merged([_r("두께")])
    review = ReviewOut(findings=[ReviewFinding(
        target_item="없는항목", verdict="상태변경", reason="x", new_status="추정")])
    readings, _a, log = apply_findings(merged, review)
    assert readings[0].status == "확정"
    assert log[0]["applied"] is False and "대상 없음" in log[0]["note"]


def test_multiple_findings_all_recorded():
    merged = _merged([_r("A"), _r("B")])
    review = ReviewOut(findings=[
        ReviewFinding(target_item="A", verdict="상태변경", reason="r", new_status="검토지적"),
        ReviewFinding(target_item="B", verdict="기각", reason="r2"),
    ])
    readings, _a, log = apply_findings(merged, review)
    assert {r.item: r.status for r in readings} == {"A": "검토지적", "B": "확정"}
    assert len(log) == 2
```

`worker/tests/test_reading_store.py`:

```python
"""DB 반영 — 계열 단위 교체(멱등). 가짜 커서로 SQL 호출 형태만 검증한다."""

import pytest

from m3d.models import AmbiguityOption
from m3d.reading.schema import LlmAmbiguity, LlmReading
from m3d.reading.store import build_rows

BBOX = [1.0, 2.0, 3.0, 4.0]


def _r(item, page_no=1):
    return LlmReading(item=item, value_raw="300", unit="mm", page_no=page_no,
                      mm_bbox=BBOX, crosscheck="합=300 ✓", status="확정")


def _a(item, page_no=1):
    return LlmAmbiguity(item=item, page_no=page_no, mm_bbox=BBOX,
                        options=[AmbiguityOption(label="a", basis="b"),
                                 AmbiguityOption(label="c", basis="d")],
                        model_impact="영향")


def test_build_rows_maps_ord_and_page():
    rows_r, rows_a = build_rows("B", "B01", [_r("두께")], [_a("해석")], round_no=3)
    assert rows_r[0].region == "B" and rows_r[0].basis_ord == "B01"
    assert rows_r[0].round == 3
    assert rows_a[0].basis_ord == "B01" and rows_a[0].status == "대기"


def test_build_rows_preserves_crosscheck_and_bbox():
    rows_r, _ = build_rows("B", "B01", [_r("두께")], [], round_no=1)
    assert rows_r[0].crosscheck == {"expr": "합=300 ✓"}
    assert rows_r[0].basis_mm_bbox == BBOX


def test_build_rows_options_serialised():
    _, rows_a = build_rows("B", "B01", [], [_a("해석")], round_no=1)
    assert [o.label for o in rows_a[0].options] == ["a", "c"]


def test_build_rows_empty_inputs():
    rows_r, rows_a = build_rows("B", "B01", [], [], round_no=1)
    assert rows_r == [] and rows_a == []
```

- [ ] **Step 2: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_reading_region.py worker\tests\test_reading_store.py -v
```

Expected: `ModuleNotFoundError: No module named 'm3d.reading.region'`

- [ ] **Step 3: `prompts.py` 에 통합·검토 프롬프트 추가**

파일 끝에 추가:

```python
MERGE_RULES = """\
당신은 한 계열(도면 묶음)의 시트별 판독 결과를 하나의 치수 정본으로 통합한다.

[통합 규칙]
- 같은 항목이 여러 시트에서 나오면 하나로 합치고, 근거는 가장 명확한 시트를 남긴다.
- 2개 이상 시트·뷰에서 일치 확인된 항목만 확정을 유지한다. 한 곳에서만 나온 값은
  추정으로 낮춘다(단일 소스 판독 금지).
- 값이 서로 다르면 그 자체가 애매성이다. **서로 다른 구간(존)을 본 것일 가능성**을
  선택지에 반드시 포함한다 — 실제로 '3열 vs 8열' 상충의 정답이 구간별 병존이었던
  사례가 있다.
- 시트별 판독의 애매성은 중복을 제거해 승계한다.
- notes 에 병합·교차확인 과정에서 판단한 근거를 남긴다.
"""

REVIEW_RULES = """\
당신은 판독 결과를 반박하는 검토자다. 판독자와 다른 사람이며, 통과시키는 것이
아니라 **틀린 곳을 찾는 것**이 임무다.

[반박 관점]
- 검산 없는 파생값, 합계가 맞지 않는 체인
- 단일 소스인데 확정으로 표기된 값
- 구간(존)이 다를 가능성을 무시하고 하나로 뭉갠 값
- 도면 간 표기 상충을 애매성으로 올리지 않고 임의 선택한 값
- 근거 좌표가 값의 위치와 어긋나 보이는 항목

[판정]
- 기각: 반박을 시도했으나 판독이 옳다. 이유를 적는다.
- 상태변경: 확정→추정 또는 검토지적으로 낮춰야 한다. new_status 필수.
- 신규애매성: 사용자에게 물어야 한다. new_ambiguity 필수(선택지 2~4·각 근거·모델 영향).

확신이 없으면 상태변경(추정)을 택한다. 추정을 확정처럼 두는 것이 가장 큰 사고다.
"""


def merge_system(region: str) -> str:
    return f"{KB_EXCERPT}\n{MERGE_RULES}\n[이 계열]\n{REGION_HINTS.get(region, '')}\n"


def review_system(region: str) -> str:
    return f"{KB_EXCERPT}\n{REVIEW_RULES}\n[이 계열]\n{REGION_HINTS.get(region, '')}\n"
```

- [ ] **Step 4: `worker/src/m3d/reading/region.py` 구현**

```python
"""계열 통합 + 적대적 검토 (설계서 §4-2·§4-3)."""

from __future__ import annotations

import json

from pydantic import ValidationError

from m3d.config import Config
from m3d.reading import prompts
from m3d.reading.cache import cache_key, cache_path, load_cached, save_cached
from m3d.reading.client import (
    MODEL_READ,
    MODEL_REVIEW,
    PROMPT_VERSION,
    build_client,
    log_usage,
)
from m3d.reading.schema import (
    LlmAmbiguity,
    LlmReading,
    RegionMergeOut,
    ReviewOut,
    SheetReadOut,
)

MAX_TOKENS = 16000


def _call(cfg, dataset, client, *, stage, region, model, system, user_text,
          out_format, key_payload, force):
    key = cache_key({**key_payload, "prompt": PROMPT_VERSION, "model": model, "stage": stage})
    path = cache_path(cfg, dataset, stage, key)
    if not force:
        cached = load_cached(path)
        if cached is not None:
            usage = {"stage": stage, "region": region, "model": model,
                     "in": 0, "out": 0, "cached": True, "retried": False}
            log_usage(cfg, dataset, usage)
            return out_format.model_validate(cached), usage

    client = client or build_client(cfg)
    retried, reason = False, None
    for attempt in (1, 2):
        content = user_text if reason is None else (
            f"{user_text}\n\n[재시도] 직전 응답이 스키마 검증에 실패했다: {reason}")
        try:
            resp = client.messages.parse(
                model=model, max_tokens=MAX_TOKENS, system=system,
                output_format=out_format,
                messages=[{"role": "user", "content": content}],
            )
            break
        except ValidationError as exc:
            if attempt == 2:
                raise
            retried, reason = True, str(exc)[:500]

    out = resp.parsed_output
    save_cached(path, out.model_dump())
    usage = {"stage": stage, "region": region, "model": model,
             "in": resp.usage.input_tokens, "out": resp.usage.output_tokens,
             "cached": False, "retried": retried}
    log_usage(cfg, dataset, usage)
    return out, usage


def merge_region(cfg: Config, dataset: str, region: str,
                 sheet_outs: list[tuple[str, SheetReadOut]], *,
                 force: bool = False, client=None):
    """sheet_outs: [(ord, SheetReadOut)] — 계열 내 전 페이지 판독."""
    payload = [{"ord": o, **out.model_dump()} for o, out in sheet_outs]
    user = ("계열 내 시트별 판독 결과다. 하나의 치수 정본으로 통합하라.\n"
            + json.dumps(payload, ensure_ascii=False))
    return _call(cfg, dataset, client, stage="merge", region=region, model=MODEL_READ,
                 system=prompts.merge_system(region), user_text=user,
                 out_format=RegionMergeOut,
                 key_payload={"payload": cache_key({"p": payload})}, force=force)


def review_region(cfg: Config, dataset: str, region: str, merged: RegionMergeOut, *,
                  force: bool = False, client=None):
    user = ("아래는 한 계열의 통합 판독 결과다. 반박을 시도하라.\n"
            + json.dumps(merged.model_dump(), ensure_ascii=False))
    return _call(cfg, dataset, client, stage="review", region=region, model=MODEL_REVIEW,
                 system=prompts.review_system(region), user_text=user,
                 out_format=ReviewOut,
                 key_payload={"merged": cache_key({"m": merged.model_dump()})}, force=force)


def apply_findings(merged: RegionMergeOut, review: ReviewOut):
    """검토 지적을 최종 결과에 반영한다. 기각도 기록으로 남긴다 (§3)."""
    readings: list[LlmReading] = [r.model_copy() for r in merged.readings]
    ambiguities: list[LlmAmbiguity] = list(merged.ambiguities)
    log: list[dict] = []

    by_item = {r.item: i for i, r in enumerate(readings)}
    for f in review.findings:
        entry = {"target_item": f.target_item, "verdict": f.verdict,
                 "reason": f.reason, "applied": False, "note": ""}
        if f.verdict == "기각":
            log.append(entry)
            continue
        if f.verdict == "상태변경":
            idx = by_item.get(f.target_item)
            if idx is None:
                entry["note"] = "대상 없음 — 항목명이 통합 결과와 다르다"
            else:
                readings[idx] = readings[idx].model_copy(update={"status": f.new_status})
                entry["applied"] = True
        elif f.verdict == "신규애매성":
            ambiguities.append(f.new_ambiguity)
            entry["applied"] = True
        log.append(entry)

    return readings, ambiguities, log
```

- [ ] **Step 5: `worker/src/m3d/reading/store.py` 구현**

```python
"""판독 결과 DB 반영 (설계서 §5) — 계열 단위 교체로 멱등을 만든다."""

from __future__ import annotations

import json

import psycopg

from m3d.config import Config
from m3d.models import AmbiguityRow, ReadingRow
from m3d.reading.schema import LlmAmbiguity, LlmReading


def build_rows(region: str, ord_: str, readings: list[LlmReading],
               ambiguities: list[LlmAmbiguity], round_no: int
               ) -> tuple[list[ReadingRow], list[AmbiguityRow]]:
    """LLM 출력 → DB 행 모델. ord 는 근거 시트로 붙는다."""
    rows_r = [
        ReadingRow(region=region, item=r.item, value_raw=r.value_raw, unit=r.unit,
                   basis_ord=ord_, basis_page_no=r.page_no, basis_mm_bbox=r.mm_bbox,
                   crosscheck={"expr": r.crosscheck} if r.crosscheck else None,
                   status=r.status, round=round_no)
        for r in readings
    ]
    rows_a = [
        AmbiguityRow(item=a.item, basis_ord=ord_, basis_page_no=a.page_no,
                     mm_bbox=a.mm_bbox, options=a.options, model_impact=a.model_impact)
        for a in ambiguities
    ]
    return rows_r, rows_a


def resolve_ids(conn, project_id):
    with conn.cursor() as cur:
        cur.execute("select ord, id from sheets where project_id = %s", (project_id,))
        sheet_ids = dict(cur.fetchall())
        cur.execute(
            "select s.ord, sp.page_no, sp.id from sheet_pages sp "
            "join sheets s on s.id = sp.sheet_id where s.project_id = %s",
            (project_id,))
        page_ids = {(o, n): i for o, n, i in cur.fetchall()}
    return sheet_ids, page_ids


def replace_region(cfg: Config, dataset: str, region: str,
                   rows_r: list[ReadingRow], rows_a: list[AmbiguityRow]) -> dict[str, int]:
    """해당 계열의 기존 행을 지우고 새로 넣는다 — 재실행이 누적되지 않게."""
    with psycopg.connect(cfg.require_db_url()) as conn:
        with conn.cursor() as cur:
            cur.execute("select id from projects where slug = %s", (dataset,))
            row = cur.fetchone()
            if row is None:
                raise RuntimeError(f"프로젝트 '{dataset}' 없음 — seed 먼저")
            project_id = row[0]
        sheet_ids, page_ids = resolve_ids(conn, project_id)

        with conn.cursor() as cur:
            cur.execute("delete from readings where project_id = %s and region = %s",
                        (project_id, region))
            ords = {r.basis_ord for r in rows_a}
            if ords:
                cur.execute(
                    "delete from ambiguities where project_id = %s and basis_sheet_id = any(%s)",
                    (project_id, [sheet_ids[o] for o in ords if o in sheet_ids]))

            for r in rows_r:
                cur.execute(
                    "insert into readings (project_id, region, item, value_raw, unit, "
                    "basis_sheet_id, basis_page_id, basis_mm_bbox, crosscheck, status, round) "
                    "values (%s,%s,%s,%s,%s,%s,%s,%s,%s,%s,%s)",
                    (project_id, r.region, r.item, r.value_raw, r.unit,
                     sheet_ids[r.basis_ord], page_ids.get((r.basis_ord, r.basis_page_no)),
                     psycopg.types.json.Json(r.basis_mm_bbox),
                     psycopg.types.json.Json(r.crosscheck) if r.crosscheck else None,
                     r.status, r.round))

            for a in rows_a:
                cur.execute(
                    "insert into ambiguities (project_id, item, basis_sheet_id, "
                    "sheet_page_id, mm_bbox, options, model_impact, status) "
                    "values (%s,%s,%s,%s,%s,%s,%s,%s)",
                    (project_id, a.item, sheet_ids[a.basis_ord],
                     page_ids.get((a.basis_ord, a.basis_page_no)),
                     psycopg.types.json.Json(a.mm_bbox),
                     psycopg.types.json.Json([o.model_dump() for o in a.options]),
                     a.model_impact, a.status))
        conn.commit()

    return {"readings": len(rows_r), "ambiguities": len(rows_a)}
```

- [ ] **Step 6: 통과 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests -q
```

Expected: **212 passed** (203 + region 5 + store 4). 실측 우선.

- [ ] **Step 7: CLI — `read` 에 통합·DB 반영을 붙이고 `review` 추가**

`read` 명령의 루프 뒤(합계 출력 전)에 계열별 통합·저장을 추가하고, `review` 명령을 새로 만든다. 두 명령의 공통 흐름:

1. `list_pages` → 페이지별 `read_sheet`
2. 계열별로 묶어 `merge_region`
3. `review` 명령일 때만 `review_region` + `apply_findings`
4. `build_rows` → `replace_region` (round: read=2, review=3)
5. 계열별 결과·비용·검토 기록을 출력하고, 검토 기록은
   `data/derived/<dataset>/review-<region>.json` 에 저장

정확한 코드는 Task 4 의 `read` 명령 형태를 따르되, 계열 루프를 바깥에 두고 실패는
수집한다(M1 확립 패턴). `estimate_cost` 는 import 블록에서 직접 가져온다.

- [ ] **Step 8: 커밋**

```bash
git add worker/src/m3d/reading/region.py worker/src/m3d/reading/store.py worker/src/m3d/reading/prompts.py worker/src/m3d/cli.py worker/tests/test_reading_region.py worker/tests/test_reading_store.py
git commit -m "feat(reading): 계열 통합 + 적대적 검토 + DB 반영

- 통합 프롬프트에 '값이 다르면 서로 다른 존일 가능성' 을 명시 (원 프로젝트 3열vs8열 사례)
- 검토는 반박이 임무 — 확신 없으면 추정으로 낮추게 지시, 기각도 기록에 남김
- apply_findings 는 순수 함수: 상태변경·신규애매성·기각·대상없음 4경로 테스트
- replace_region 으로 계열 단위 교체 (재실행이 누적되지 않음)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## Task 6: 크롭 생성 `m3d crops` (결정론)

**Files:**
- Create: `worker/src/m3d/reading/crops.py`
- Modify: `worker/src/m3d/cli.py` (`crops` 명령)
- Test: `worker/tests/test_reading_crops.py`

**Interfaces:**
- Consumes: `m3d.convert.frames`(M1), PIL, psycopg
- Produces:
  - `m3d.reading.crops.CONTEXT_MM: float` (= 40.0) — 주변 맥락 여백
  - `m3d.reading.crops.MIN_CROP_PX: int` (= 400)
  - `m3d.reading.crops.mm_bbox_to_px(bbox_mm, paper_mm, png_size, context_mm=CONTEXT_MM) -> tuple[int,int,int,int]` — 순수
  - `m3d.reading.crops.render_crop(png: Path, box_px, out: Path, min_px=MIN_CROP_PX) -> tuple[int,int]`
  - `m3d.reading.crops.run_crops(cfg, dataset, *, force=False) -> dict`

- [ ] **Step 1: 실패하는 좌표 변환 테스트 작성**

`worker/tests/test_reading_crops.py`:

```python
"""ambiguity mm bbox → 크롭 픽셀. M1 의 용지 mm 계약을 회수하는 지점 (설계서 §6)."""

import pytest
from PIL import Image

from m3d.reading.crops import CONTEXT_MM, mm_bbox_to_px, render_crop

PAPER = [1189.0, 841.0]
SIZE = (8000, 5659)          # M1 렌더 실측 비율에 가까운 값


def test_center_maps_to_center():
    x0, y0, x1, y1 = mm_bbox_to_px([594.5, 420.5, 594.5, 420.5], PAPER, SIZE, context_mm=0)
    assert x0 == pytest.approx(4000, abs=3) and x1 == pytest.approx(4000, abs=3)
    assert y0 == pytest.approx(2829, abs=3)


def test_y_axis_is_flipped():
    """용지 y 는 아래에서 위로, 픽셀 y 는 위에서 아래로 — 뒤집혀야 한다."""
    top = mm_bbox_to_px([100, 800, 200, 830], PAPER, SIZE, context_mm=0)
    bottom = mm_bbox_to_px([100, 10, 200, 40], PAPER, SIZE, context_mm=0)
    assert top[1] < bottom[1]


def test_context_margin_expands_box():
    tight = mm_bbox_to_px([500, 400, 520, 420], PAPER, SIZE, context_mm=0)
    padded = mm_bbox_to_px([500, 400, 520, 420], PAPER, SIZE, context_mm=CONTEXT_MM)
    assert padded[0] < tight[0] and padded[2] > tight[2]
    assert padded[1] < tight[1] and padded[3] > tight[3]


def test_clamped_to_image_bounds():
    box = mm_bbox_to_px([0.0, 0.0, 5.0, 5.0], PAPER, SIZE, context_mm=CONTEXT_MM)
    assert box[0] >= 0 and box[1] >= 0
    assert box[2] <= SIZE[0] and box[3] <= SIZE[1]


def test_degenerate_point_bbox_still_has_area():
    """LLM 이 점 하나를 주더라도 맥락 여백이 면적을 만든다."""
    x0, y0, x1, y1 = mm_bbox_to_px([600, 400, 600, 400], PAPER, SIZE)
    assert x1 > x0 and y1 > y0


def test_render_crop_upscales_small(tmp_path):
    src = tmp_path / "s.png"
    Image.new("RGB", (2000, 1500), "white").save(src)
    out = tmp_path / "c.png"
    w, h = render_crop(src, (100, 100, 250, 200), out)
    assert min(w, h) >= 400          # 너무 작으면 확대해서 읽히게
    with Image.open(out) as im:
        assert im.size == (w, h)


def test_render_crop_keeps_large_as_is(tmp_path):
    src = tmp_path / "s.png"
    Image.new("RGB", (4000, 3000), "white").save(src)
    out = tmp_path / "c.png"
    w, h = render_crop(src, (0, 0, 2000, 1500), out)
    assert (w, h) == (2000, 1500)
```

- [ ] **Step 2: 실행해 실패 확인**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests\test_reading_crops.py -v
```

Expected: `ModuleNotFoundError: No module named 'm3d.reading.crops'`

- [ ] **Step 3: `worker/src/m3d/reading/crops.py` 구현**

```python
"""ambiguity 크롭 생성 (설계서 §6) — 질문 카드의 이미지.

지식베이스 §4: 질문에는 반드시 해당 도면의 크롭 이미지를 동반한다. 좌표는 용지 mm
로 받아 M1 렌더 PNG 픽셀로 변환한다 — M1 이 sheet_pages 에 width_px/height_px 를
채워둔 이유가 여기서 회수된다.
"""

from __future__ import annotations

from pathlib import Path

import psycopg
from PIL import Image

from m3d.config import Config
from m3d.samples.manifest import sha256_file

Image.MAX_IMAGE_PIXELS = None

CONTEXT_MM = 40.0     # 주변 맥락 — 값만 잘라내면 무엇의 치수인지 알 수 없다
MIN_CROP_PX = 400     # 너무 작은 크롭은 확대해서 읽히게 한다


def mm_bbox_to_px(bbox_mm, paper_mm, png_size, context_mm: float = CONTEXT_MM):
    """용지 mm bbox → 픽셀 box (좌상단 원점). y 축은 뒤집는다."""
    x0m, y0m, x1m, y1m = bbox_mm
    x0m, x1m = min(x0m, x1m) - context_mm, max(x0m, x1m) + context_mm
    y0m, y1m = min(y0m, y1m) - context_mm, max(y0m, y1m) + context_mm

    pw, ph = paper_mm
    iw, ih = png_size
    sx, sy = iw / pw, ih / ph

    left = int(round(x0m * sx))
    right = int(round(x1m * sx))
    top = int(round((ph - y1m) * sy))      # y 뒤집기
    bottom = int(round((ph - y0m) * sy))

    left, top = max(0, left), max(0, top)
    right, bottom = min(iw, right), min(ih, bottom)
    if right <= left:
        right = min(iw, left + 1)
    if bottom <= top:
        bottom = min(ih, top + 1)
    return left, top, right, bottom


def render_crop(png: Path, box_px, out: Path, min_px: int = MIN_CROP_PX) -> tuple[int, int]:
    with Image.open(png) as im:
        crop = im.crop(box_px).convert("RGB")
        w, h = crop.size
        if min(w, h) < min_px and min(w, h) > 0:
            scale = min_px / min(w, h)
            w, h = int(round(w * scale)), int(round(h * scale))
            crop = crop.resize((w, h), Image.LANCZOS)
        out.parent.mkdir(parents=True, exist_ok=True)
        crop.save(out)
        return w, h


def run_crops(cfg: Config, dataset: str, *, force: bool = False) -> dict:
    """대기 중 ambiguity 전건에 크롭을 만들고 crop_rel_path·assets 를 채운다."""
    made, skipped, failures = 0, 0, []
    with psycopg.connect(cfg.require_db_url()) as conn:
        with conn.cursor() as cur:
            cur.execute("select id from projects where slug = %s", (dataset,))
            project_id = cur.fetchone()[0]
            cur.execute(
                "select a.id, a.mm_bbox, a.crop_rel_path, s.ord, sp.page_no, "
                "       sp.width_px, sp.height_px "
                "from ambiguities a "
                "join sheets s on s.id = a.basis_sheet_id "
                "left join sheet_pages sp on sp.id = a.sheet_page_id "
                "where a.project_id = %s order by s.ord, sp.page_no",
                (project_id,))
            rows = cur.fetchall()

        for amb_id, bbox, existing, ord_, page_no, wpx, hpx in rows:
            if existing and not force:
                skipped += 1
                continue
            try:
                stem = next((cfg.derived_dir / dataset / "png").glob(f"{ord_}_*_p{page_no}.png"))
                text_json = (cfg.derived_dir / dataset / "text" / f"{stem.stem}.json")
                import json as _json
                paper = _json.loads(text_json.read_text(encoding="utf-8"))["paper_mm"]
                box = mm_bbox_to_px(bbox, paper, (wpx, hpx))
                rel = f"data/derived/{dataset}/crops/{amb_id}.png"
                out = cfg.repo_root / rel
                render_crop(stem, box, out)
                with conn.cursor() as cur:
                    cur.execute("update ambiguities set crop_rel_path = %s where id = %s",
                                (rel, amb_id))
                    cur.execute(
                        "insert into assets (project_id, kind, role, rel_path, bytes, sha256) "
                        "values (%s,'png','derived',%s,%s,%s) "
                        "on conflict (project_id, rel_path) do update set "
                        "bytes = excluded.bytes, sha256 = excluded.sha256",
                        (project_id, rel, out.stat().st_size, sha256_file(out)))
                made += 1
            except Exception as exc:
                failures.append((str(amb_id), f"{type(exc).__name__}: {exc}"))
        conn.commit()

    return {"made": made, "skipped": skipped, "failures": failures, "total": len(rows)}
```

- [ ] **Step 4: 통과 확인 + CLI `crops` 추가**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\python.exe -m pytest worker\tests -q
```

Expected: **219 passed** (212 + crops 7).

`cli.py` 에 `crops` 명령 추가 (`catalog` 형태를 따른다):

```python
@app.command()
def crops(
    dataset: str = typer.Argument(..., help="데이터셋 슬러그"),
    force: bool = typer.Option(False, "--force", help="이미 있어도 재생성"),
) -> None:
    """[5 준비] ambiguity 크롭 생성 — 질문 카드의 이미지 (설계서 §6)."""
    cfg = load_config()
    r = crops_run.run_crops(cfg, dataset, force=force)
    typer.echo(f"크롭 {r['made']}건 생성 / {r['skipped']}건 스킵 / 전체 {r['total']}건")
    for aid, err in r["failures"]:
        typer.echo(f"  실패 {aid}: {err}")
    raise typer.Exit(code=1 if r["failures"] else 0)
```

- [ ] **Step 5: 커밋**

```bash
git add worker/src/m3d/reading/crops.py worker/src/m3d/cli.py worker/tests/test_reading_crops.py
git commit -m "feat(reading): ambiguity 크롭 생성 (결정론)

- 용지 mm → 픽셀 변환, y 축 뒤집기·맥락 여백 40mm·경계 clamp·점 bbox 면적 보장
- 작은 크롭은 400px 까지 확대 (질문 카드에서 읽혀야 한다)
- M1 의 sheet_pages.width_px 와 sheet_text.paper_mm 을 회수하는 지점

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## Task 7: 시범 B계열 완주 → 사용자 승인 게이트

> **이 태스크는 사용자 승인으로 끝난다.** 승인 없이 Task 8 로 넘어가지 않는다 (설계서 §7-3).

**Files:**
- Create: `data/derived/ab1-p4p5/pilot-report.md` (gitignore — 게이트 자료)
- Modify: 필요 시 `prompts.py`(조정) + `client.PROMPT_VERSION` 올림

- [ ] **Step 1: B계열 판독 실행 (실호출, <$0.5)**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe read ab1-p4p5 --region B
```

Expected: B01~B03 6페이지 처리, 계열 통합까지, 비용 출력. 실패 0.

- [ ] **Step 2: 적대적 검토 실행 (<$0.5)**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe review ab1-p4p5 --region B
```

Expected: 검토 지적 목록 + 반영/기각 기록, `review-B.json` 저장.

- [ ] **Step 3: 크롭 생성**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe crops ab1-p4p5
```

Expected: B계열 ambiguity 전건에 크롭 PNG 생성, 실패 0.

- [ ] **Step 4: 멱등 재실행 확인 (무과금)**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe read ab1-p4p5 --region B
```

Expected: 전건 `캐시`, 합계 비용 `$0.0000`.

- [ ] **Step 5: 게이트 자료 작성 — `pilot-report.md`**

다음을 포함한다:
1. **비용**: `usage.jsonl` 집계 (단계별 토큰·비용 합계)
2. **readings 표**: 상위 15건 — 항목·값·상태·근거(시트 p·mm)·검산
3. **ambiguities 전건**: 항목·선택지(각 근거)·모델 영향·크롭 파일 경로
4. **검토 기록**: 지적별 판정·사유·반영 여부
5. **B계열 정답 대조**: 설계서 §1의 Q2(슬래브 기입두께)·Q3(보도 폭원)이
   ambiguity 로 잡혔는가 — **예/아니오와 근거**
6. **DB 상태**: `select region, status, count(*) from readings group by 1,2` 결과

- [ ] **Step 6: 크롭 육안 확인 (전역 규칙 §2)**

ambiguity 크롭 2~3장을 열어 **질문 카드로 쓸 수 있는지** 확인한다 — 해당 값이
보이는가, 주변 맥락이 충분한가, 엉뚱한 영역을 잘랐는가. 결과를 리포트에 기록한다.

- [ ] **Step 7: 사용자에게 보고하고 승인 대기 — STOP**

`pilot-report.md` 요약과 크롭 이미지를 사용자에게 제시하고 **판단을 받는다**:
- 승인 → Task 8 확산
- 조정 요청 → `prompts.py` 수정 + `PROMPT_VERSION` 올림 → Step 1 부터 재실행
  (캐시 키가 바뀌어 재호출되고, 이전 결과는 캐시에 남아 비교 가능)

**승인 없이 Task 8 을 시작하지 않는다.**

---

## Task 8: 전 계열 확산 + 합격 판정

**Files:**
- Create: `scripts/verify-m2a.ps1`
- Create: `data/derived/ab1-p4p5/acceptance.md` (gitignore)
- Modify: `worker/src/m3d/db.py` + `cli.py` (`db check` 에 판독 통계 추가)
- Modify: `README.md` (M2a 명령 추가)

- [ ] **Step 1: `db check` 확장**

`check()` 에 추가:

```python
        cur.execute("select region, status, count(*) from readings group by 1, 2 order by 1, 2")
        reading_stats = [{"region": r, "status": s, "n": n} for r, s, n in cur.fetchall()]

        cur.execute("select status, count(*) from ambiguities group by 1 order by 1")
        ambiguity_stats = dict(cur.fetchall())

        cur.execute("select count(*) from ambiguities where crop_rel_path is not null")
        crops_ready = cur.fetchone()[0]
```

반환 dict 에 `reading_stats`·`ambiguity_stats`·`crops_ready` 추가하고, `db_check`
CLI 에 출력 블록을 더한다(기존 출력은 그대로 둔다).

- [ ] **Step 2: 전 계열 확산 실행 (실호출, ~$8)**

```powershell
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe read ab1-p4p5
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe review ab1-p4p5
$env:PYTHONUTF8='1'; .\worker\.venv\Scripts\m3d.exe crops ab1-p4p5
```

Expected: 61페이지·6계열 완주, 실패 0. B계열은 캐시 히트(시범에서 이미 실행).

- [ ] **Step 3: 합격 판정 ① — 결정 4건 대응 ambiguity 검출 (설계서 §8-4)**

DB 의 ambiguities 전건을 SPEC_v2 의 사용자 결정 4건과 대조한다:

| 결정 | 요소 | 기대 계열 |
|---|---|---|
| Q1 | 형고 4,000/2,800 이 내공인가 전강고인가 | C |
| Q2 | 슬래브 기입두께가 콘크리트 순두께인가 | B |
| Q3 | 보도 2,950 + 중앙방호벽 450 폭원 구성 | B |
| Q4 | 받침 형식(면진) 표기 상충 | A |

**같은 요소를 묻고 있는가**를 사람이 판정한다(문구 일치가 아니라 요소 일치).
4/4 가 목표. 미검출 건은 원인을 분석해 `acceptance.md` 에 기록한다 —
프롬프트 조정으로 잡히면 `PROMPT_VERSION` 올려 해당 계열만 재실행.

- [ ] **Step 4: 합격 판정 ② — readings 표본 20건 대조 (§8-5)**

`data/fixtures/ab1-p4p5/접속1교_실측정리.md` §1~§8 에서 **20건을 뽑아** DB readings
와 값을 대조한다. 불일치는 어느 쪽이 맞는지 근거로 판정한다 — 실측정리가 틀린
사례도 있다(§9-2: 복부 실판 높이 종결 사례). 결과를 표로 `acceptance.md` 에 기록.

- [ ] **Step 5: 합격 판정 ③~⑤**

- 검토 기록: `review-*.json` 전건이 반영 또는 사유付 기각으로 종결됐는가
- 크롭: ambiguity 전건에 `crop_rel_path` + 파일 실재 + assets 등재
- 멱등: read·review 재실행이 전건 캐시 히트로 무과금 완주

- [ ] **Step 6: `scripts/verify-m2a.ps1` 작성**

Task 8 의 검증 명령들을 묶는다 — M1 의 `verify-m0.ps1` 패턴 계승(UTF-8 BOM 필수,
`$global:LASTEXITCODE = 0` 초기화, doctor 형 소프트 실패는 출력 패턴으로 감지).
포함: pytest / `db check` / read·review 멱등 재실행 / crops 파일 실재 확인.

- [ ] **Step 7: `README.md` 갱신**

시작하기에 3줄 추가:

```
   .\worker\.venv\Scripts\m3d.exe read ab1-p4p5
   .\worker\.venv\Scripts\m3d.exe review ab1-p4p5
   .\worker\.venv\Scripts\m3d.exe crops ab1-p4p5
```

`.env` 항목에 `ANTHROPIC_API_KEY`·`ANTHROPIC_WORKSPACE_ID` 를 추가하고,
**API 비용이 발생하는 명령임을 명시**한다.

- [ ] **Step 8: `acceptance.md` 작성 + 커밋**

합격 기준 8항목(설계서 §8)의 결과를 증거와 함께 기록한다. 4·5 가 기대와 다르면
**기대값을 고치지 말고** 원인 분석을 싣는다.

```bash
git add worker/src/m3d/db.py worker/src/m3d/cli.py scripts/verify-m2a.ps1 README.md
git commit -m "feat(m2a): 전 계열 확산 + db check 판독 통계 + 검증 스크립트

- 61페이지 6계열 판독·검토·크롭 완주 (비용 usage.jsonl 기록)
- 합격: 결정 4건 대응 ambiguity 검출 / readings 표본 20건 대조 / 검토 전건 종결
- db check 에 계열·상태별 readings, ambiguity 상태, 크롭 준비 수 추가

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01AZoYNTVPox7g1d6oJd5Wo1"
```

---

## 계획 자체 검토 결과

### 1. 스펙 커버리지

| 설계서 절 | 담당 태스크 |
|---|---|
| §2-1 API 구성(헤더·output_format) | 2 (client.py + 주석에 실측 근거) |
| §3 CLI 3명령·캐시·usage 로그 | 4(read) · 5(review) · 6(crops) · 2(usage) |
| §4-1 시트 판독(입력 조립·스키마 강제·재시도) | 3 · 4 |
| §4-2 계열 통합(교차확인·존 병존) | 5 |
| §4-3 적대적 검토(반박·기각 기록) | 5 |
| §5 0003 스키마 | 1 |
| §6 크롭(mm→px·맥락 여백) | 6 |
| §7 시범→게이트→확산 | 7 · 8 |
| §8 합격 기준 8항목 | 8 (①~⑤) + 7(시범 부분) |
| §9 범위 밖 | 어느 태스크에도 카드 UI·decisions·분류·Batch 없음 — 확인 |
| §10 리스크 | 비결정성→PROMPT_VERSION+게이트 / 리사이즈→sheet_text 1차 소스 / 대형 텍스트→절단(글자 높이 순) / 과금→캐시·usage·시범 |

**빠진 것 없음.**

### 2. 플레이스홀더 스캔

Task 5 Step 7 과 Task 8 Step 6 은 "형태를 따른다"는 서술형이다 — 둘 다 **선행
태스크에 완전한 참조 구현이 있고**(Task 4 의 read 명령, M1 의 verify-m0.ps1),
포함 항목을 명시했다. 나머지 스텝은 전부 완전한 코드를 담았다. TBD·TODO 없음.

### 3. 타입 일관성 (교차 확인)

- `LlmReading`/`LlmAmbiguity`(Task 3) ↔ `SheetReadOut`(3) ↔ `read_sheet` 반환(4)
  ↔ `merge_region` 입력(5) ↔ `build_rows`(5) 일치.
- `ReadingRow`/`AmbiguityRow`(Task 1) ↔ `build_rows` 생성부 ↔ `replace_region`
  INSERT 컬럼 순서 일치 (basis_ord→sheet_id, basis_page_no→page_id 해석).
- `mm_bbox` 는 전 구간 `[x0,y0,x1,y1]` 용지 mm — Task 6 의 `mm_bbox_to_px` 가
  같은 형태를 받는다.
- 캐시 키: `read`(png·text sha) / `merge`(payload 해시) / `review`(merged 해시) —
  전부 `PROMPT_VERSION`·모델 포함.
- 테스트 누적: 144 → 160 → 172 → 196 → 203 → 212 → 219 (각 태스크 명시,
  디스패치 시점 실측 우선 원칙은 Global Constraints).

### 4. 과금 규율 검산

실호출이 있는 스텝: Task 2 Step 8(<$0.01) · Task 4 Step 7(<$0.05) ·
Task 7 Step 1~3(<$1) · Task 8 Step 2(~$8). 그 외 모든 테스트는 가짜 클라이언트
또는 순수 로직 — **테스트 실행으로는 과금되지 않는다.** 크레딧 $20.28 대비 여유.
